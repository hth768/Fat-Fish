# 2026-09-18 工作日志

## 修复：选完主题色再调回外观页会跳回深夜蓝
- 现象：用户在外观页选了某个主题色，没点「保存外观」就切到别的页面，再进外观页时主题又变回「暗夜蓝（midnight）」。
- 根因：`webui/app.js` 的侧栏导航 `switchSection`（行 45-47）里 `appearance: loadAppearance`——**每次进入外观页都会调用 `loadAppearance()`**，它重新 `GET /api/appearance` 并 `applyVars` 覆盖当前主题。而选主题色的点击处理只把 `appearance.theme` 写入内存变量并 `previewTheme()` 临时预览，**并未持久化**；只有点「保存外观」才会 POST 到后端。于是未保存就切走、再进外观页即从后端拉回旧主题（默认 midnight）→ 跳回。
- 修复：`webui/app.js` 新增 `saveTheme(name)`（`POST /api/appearance {theme}`），并在 `renderThemeGrid` 的主题色点击回调里于 `previewTheme` 之后调用 `saveTheme(appearance.theme)`——**选主题即时持久化**。后端 `save(theme=...)` 只更新 theme、不动 title，安全。这样无论是否点「保存外观」，切走再回来都是新主题。
- 验证：重启 E:/feiyu_app 实例（旧 24256 被杀，新 PID 19336 监听 8900）后，`POST {theme:forest}` 保存，再 `GET /api/appearance`（模拟进外观页 loadAppearance）返回 `theme=forest`、`--bg=#0c1611`（森林绿调，非 midnight），确认不再跳回；HTTP 返回的 `app.js` 含 `function saveTheme`。测试后已把持久化主题恢复为默认 midnight。
- 注意：之前多次重启发现「监听 8900 的 PID 与新启动实例不符」——因 server 改为端口独占绑定，若旧实例仍在（且 cmdline 匹配未命中 `feiyu_app\app.py` 的筛选），新实例会启动即退出；需用 `netstat` 找到 8900 监听 PID 精准杀掉再起。
- 文件：`webui/app.js`（前端，无后端改动，后端 save/get_appearance 本就正确用 theme）。

## 构建助手升级为「能读文件 / 有历史 / 能改进」的 Agent
- 需求：用户要求把「构建助手」做成能读取文件、有历史文件、能改进的 Agent，并推送。
- 现状：构建助手 = `bridge/builder_api.py` 经 LLM 一句话生成 agent.json / 插件包并落盘（`server.py` 路由 `/api/builder/*`）。
- 改动：
  - `bridge/builder_api.py`：① 文件上下文 `list_context_file`/`read_context_file`（`_is_allowed_context_path` 用前缀匹配 `libs/qq_bot_runtime`/`bridge`/`webui`/`plugins` 白名单 + 大小上限，路径守卫坑：最初用顶级目录 `libs` 匹配 `libs/qq_bot_runtime` 失败，已改为前缀匹配）；② 构建历史持久化 `data/builder_history.jsonl`（jsonl 追加，`append_history`/`get_history`/`_trim_history`，每条记录 event/mode/ok/requirement/key/artifact）；③ 新增 `improve_agent`/`improve_plugin`，读取磁盘已有产物按指令改进（校验同生成流程）；④ 生成时经 `_history_block` 注入最近 2 条同类型成功案例作为 few-shot 改进范例（use_history 默认开）。
  - `server.py`：新增 `/api/builder/files`、`/api/builder/file/read`、`/api/builder/history`，并给 generate/improve 接口加 `context_paths` + `use_history`。
  - `webui/index.html`+`app.js`+`styles.css`：构建页新增「参考文件」面板（勾选源码作上下文）与「构建历史」面板（每条可「载入」/「改进」；「改进」会进入改进模式，输入框预填「改进：<原需求>」、生成键变「改进生成」）。`builder` 对象新增 `contextPaths`/`improve` 状态。
- 验证：`py_compile` 通过；纯函数测试 `list_context_files`(149 文件)、`_context_block`、路径守卫 True/False 正确；mock LLM 跑通 `improve_agent`（落盘 1 条 history，`improved_from=feiyu`），`improve_plugin` 对不存在插件正确报错。
- 提交 `d6b30f4` 并推送 `35e4f91..d6b30f4 main -> main`。
- 注意：本次临时测试脚本 `_tmp_build_test.py` 因删除需批准且超时未删，仍留工作区（未跟踪，不会被提交）；`data/builder_history.jsonl` 被 `/data/` 忽略。

## 构建助手：增加工作区读写（类代码 Agent，可反复改/debug/升级）
- 需求：用户在构建助手里改类文件、能像 codebuddy 等代码 Agent 一样对插件代码反复修改/debug/升级。
- 实现（提交 `b4e3bcf`，+421 行）：
  - `bridge/builder_api.py` 新增 5 个 `*_sync` 函数：`list_workspace_sync(dir)`、`read_workspace_sync(path)`、`write_workspace_sync(path, content, backup=True)`、`workspace_diff_sync(path, content)`、`list_plugin_files_sync(name)`。
  - `server.py` 新增 5 个路由：`/api/builder/workspace/{list,read,write,diff}`、`/api/builder/plugin/files`。
  - 安全：`_WORKSPACE_ROOTS = (plugins, libs, bridge, webui, agents, config)` 白名单；`_WORKSPACE_DENY` 强制拒绝 `.codebuddy/`、`.env`、`ai_providers.json`、`settings_store.py` 等；二次 `realpath` 校验防越界；`_is_text_file` 拒二进制；单文件 512KB 上限。
  - 自动备份：每次写之前 `shutil.copy2` 到 `data/builder_bak/<rel>.<YYYYMMDD_HHMMSS_microsec>`，每文件最多 20 份，超出按 mtime 删。
  - 前端：构建助手页末新增「工作区源码读写」面板：左栏 = 路径输入 + 快捷根（plugins/bridge/webui/libs）+ 文件/目录双击进入；右栏 = 路径 + textarea + 读/写/查看 diff；底部 details = 按插件名载入包内所有文件（一键跳到该文件）。
- 验证：
  - 白名单路径可读写可 diff（如 `plugins/bilibili_learn/manifest.json` 实际读出 453B）。
  - 黑名单全部拒绝（`.codebuddy/...`、`E:/qq_bot/ai_providers.json`、`qq_bot/ai_providers.json`）。
  - 自动备份实测：首次写 `bridge/builder_api.py`（测试脚本误操作！）备份到 `data/builder_bak/bridge/builder_api.py.20260918_174326_479530`，然后从备份完整还原 1024 行；之后用 `plugins/groups.json`（原不存在）写测试，`backup=null`，写入后 `Remove-Item` 还原。
  - 实测对 bilibili_learn 真实插件包 `list_plugin_files_sync` 返回 `manifest.json`(453B) + `plugin.py`(334B)，`read_workspace_sync` 完整读出 manifest 内容；接口耗时 < 100ms。
- 同步到运行时实例 `E:/feiyu_app` 4 个文件 + 重启（PID 15892）；E:/feiyu_app 实际 APP_DIR = feiyu_app 自身，运行时插件在 `E:/feiyu_app/plugins/`（由 qq_bot 引擎同步注入），与开发副本 `feiyu_standalone/plugins/`（空）不对称——UI 默认进入目录从 `libs` 改为 `bridge`（运行时 feiyu_app 没 libs，但有 bridge/webui/plugins）。
- 顺手清理：删除 `libs/qq_bot_runtime/{agents/agent_20260917010926, agents/ext_x, data, important_notes.json, knowledge_base.json, user_profiles.json}` 9-17 智能体导入测试残留运行态污染（`git status -s` 现只剩本次 5 个改动）。

## 推送：构建助手两轮升级 + 记忆文档
- 用户要求「推送构建助手改动以及之前未提交的改动」。核查后：工作区完全干净（`--untracked-files=all` 无输出），无任何未提交改动；此前会话残留的运行数据文件已在上条清理中删除，从未进入版本控制。
- 当时本地领先远端 3 个提交，全部推送（`d6b30f4..e088e30 main -> main`）：`b4e3bcf` 构建助手工作区读写能力、`982c37c`+`e088e30` 记忆文档（MEMORY.md / 2026-09-18.md）。`d6b30f4`（上下文/历史/改进）上一轮已推送。
- 推送前安全核查：`git diff origin/main..HEAD` 扫描 `sk-*`/`AIza*`/`ghp_*`/`Bearer` 等密钥特征，无命中；MEMORY.md 新增段仅描述能力与路径，无密钥。
- 备注：`.codebuddy/memory/*.md` 自 09-14 起即被仓库跟踪，记忆文档提交属既有惯例。

## 同步构建助手改动到部署版 E:/feiyu_app + 顺手修复 GET 路由 bug
- 用户：「把改动同步到独立版」。核查结论：`e:\qq_bot` 是**纯引擎副本**（无 `bridge/`/`server.py`/`app.py`/`plugins/`，其 `webui/` 是引擎自带控制台，09-08 的老文件、无构建助手代码），**无法承载 App 层改动**；唯一含 App 层且正在运行的是 `E:/feiyu_app`（进程 `E:\qq_bot\venv\...\python.exe E:\feiyu_app\app.py --with-core`）。故同步目标取 E:/feiyu_app。
- 比对：`bridge/builder_api.py`、`server.py`、`webui/index.html` 已一致（上一轮 17:40 同步）；落后的 6 个文件已备份到 `E:\feiyu_app\data\_sync_bak_20260918_182326\` 后同步：
  - `webui/app.js`（含默认工作区目录 `libs`→`bridge` 修复——部署版无 libs 目录，旧的 libs 版会点空）
  - `webui/styles.css`（构建助手 参考文件/历史 样式块）
  - `bridge/plugins_api.py`（3.0KB/09-12 → 24.3KB）、`bridge/pkg_manager.py`（12.9KB → 14.4KB）
  - `plugins/groups.json`、`README.md`
  - **必要性**：已同步的新 `server.py` 会调用 `group_action`/`market_list`/`market_seek`/`market_action`/`plugin_config_view`/`plugin_config_save`，而部署版旧 `plugins_api.py` 完全没有这些函数 → 插件页分组/市场/配置功能此前实为坏的。
- **发现并修复真实 bug**：`/api/builder/{files,file/read,history}` 三个只读接口被误注册在 `do_POST` 分支，而前端用 `GET()` 调用 → 「参考文件」「构建历史」面板 404。已移到 `do_GET`（`?path=` / `?limit=` 传参），`do_POST` 只留写操作。提交 `2573bb7`，推送 `471f01e..2573bb7`。
- 重启两次实例（第一次载入新 bridge 模块，第二次载入修复后 server.py）：最终 PID 8732 监听 8900。
- 实测通过：`GET /api/builder/files` → `ok:true`（149 文件）、`GET /api/builder/history` → `ok:true`、`GET /api/plugins` 10KB 含 console_platform（证明新 plugins_api 工作）、`GET /api/status` core:true、静态资源含 `_bldRefreshHistory` / `_wsLoadDir("bridge")` / `ctx-files`。

## 构建助手整合为 Codex / WorkBuddy 式对话界面（工具循环 Agent）
- 用户：「将构建助手的功能整合，改为像 codex/workbuddy 那样的界面」。原界面是 6 个堆叠面板（需求生成/参考文件/智能体接入/生成结果/构建历史/工作区读写），要整合成一个对话框。
- 架构：把已有能力包装成 **15 个 LLM 工具**（list_dir / read_file / write_file / diff_file / search_code / check_syntax / list_agents / list_plugins / list_history / generate_agent / generate_plugin / improve_agent / improve_plugin / save_agent / save_plugin），在 `run_chat` 里跑多轮 tool-calling 循环（上限 14 步），模型自主决定顺序 → 实现「说需求 → 自己看代码 → 自己改 → 自己查语法 → 自报结果」的闭环。
  - 生成类工具产物只进 `state["drafts"]`（草稿），UI 渲染草稿卡片，用户点「保存到系统」或模型显式调 `save_*` 才落盘。
  - 会话持久化 `data/builder_chat/<id>.json`（messages/drafts/title/updated），支持历史会话继续对话；历史消息只存原始用户文本，上下文文件正文不落盘（防膨胀）。
  - **关键坑**：`capability="tools"` 在本机供应商配置里**没有路由**（`ai_providers.json` 只有 chat/reasoning/vision）→ 直接调用报「无可用供应商」。已在 `_chat_with_tools` 里做回退：工具路由不可用时用 `capability="chat"` 发同一请求（`_build_payload` 本来就会带 tools，OpenAI 兼容路径会返回含 `tool_calls` 的 message dict）。
  - `think` 传 "low/medium/high" 是有效的（`_think_level` 支持三档，low 不触发 think_body）。
- 前端：`#page-builder` 改为三栏——左栏工作区文件树（点击进入目录/打开文件、行内「＋参考」）/构建历史页签，中栏对话框（工具调用卡片可展开看参数与结果、diff 内联展示、草稿卡片可编辑后保存），右栏文件编辑器（读 → diff → 保存，自动备份）。原表单式「导入智能体 / 接入外部 API」收进底部「更多」details（保留原 id 以复用绑定）。
- 关键实现细节：`#page-builder` 用 `height: calc(100vh - 72px)` + flex 让三栏各自内部滚动；旧 `builder` 对象与 `_bld*` / `_ws*` 函数全部删除，初始化统一为 `initBuilderUI()`；`loadBuilder()` 保留为导航入口。
- 验证：py_compile + `node --check app.js` 通过；31 个 `bc*` id 与 HTML 全部对应；本地逻辑单测（15 工具 / list_dir / search_code 命中 run_chat / check_syntax 正反例 / 会话 CRUD）；**部署版真实 LLM 端到端**：①「列 plugins 子目录」→ 1 步 list_dir ✓ ②「读 greeting_demo 说明作用」→ read_file ✓ ③「新建 plugins/_bc_scratch.txt 并复查」→ write_file + read_file 两步 ✓ 文件内容正确；会话落盘与 `GET /api/builder/session` 回读一致。
- 提交 `bcafa09`（5 文件，+1158/-540），推送 `954c9c3..bcafa09`；已同步部署版并重启（测试产物 `plugins/_bc_scratch.txt`、测试会话与历史已清理）。

## 构建助手按 WorkBuddy 模式重构：权限模式 + 工作区位置 + 高危确认（提交 e4cca55）
- 用户：「构建助手的运作方式按照 workbuddy 设计，设置访问和操作权限，设置工作区位置，高危操作需求确认等」。
- 三层护栏（`bridge/builder_api.py`，设置存 `data/builder_settings.json`）：
  1. **工作区位置** `settings["workspace"]`（空=APP_DIR）。`workspace_root()`/`is_custom_workspace()` 统一入口；`_resolve_rooted`、`list_workspace_sync`、`_search_workspace`、`list_context_files`、`read_context_file`、`_is_allowed_context_path` 全部改为基于工作区根。应用目录模式仍限 `_WORKSPACE_ROOTS` 白名单根；自定义工作区放行整棵树但仍受 `_WORKSPACE_DENY` + `deny_extra` + realpath 越界校验（`..` 逃逸被拒）。
  2. **权限模式** `PERM_MODES = plan / default / acceptEdits / bypassPermissions`（`_MODE_DESC` 有中文说明，UI 下拉直接展示）。闸门在 `_gate_operation(tool, args, state)`，进 `_exec_tool` 最前面；`WRITE_TOOLS = ("write_file","save_agent","save_plugin")` 才受管。
     - plan → 直接返回 `{"blocked": True}`（提示模型只给方案、让用户改权限）；
     - default → 所有写入排队确认；acceptEdits → `classify_operation` 判定 `level=="high"` 才排队，low 直接放行；bypass → 全放行。
  3. **高危确认**：`classify_operation` 把 (a) 覆盖已有文件（`confirm_overwrite`）(b) 核心代码（`_sensitive_path`：顶层脚本 + `bridge/ libs/ webui/ runtime/ server/`，`confirm_sensitive`）(c) 智能体/插件落盘（`confirm_install`）判为 `high`。命中即 `_queue_approval` → 存 `data/builder_approvals.json`，返回给模型 `{"pending": True, "approval_id", "message": "…不要重复提交"}`。
     - `approve_approval_sync(bridge, aid)` 才真正执行（write_file 直接写；save_agent/save_plugin 走 `bridge.lt.run_coro`），结果通过 `_note_session()` 以 `[系统通知]` 的 user 消息写回会话 → **模型下一轮能看到已批准/被拒绝**（避免重复提交或再次尝试）。
     - `reject_approval` / `clear_approvals` 同理。
- 新增工具 `get_builder_settings`（模型可自查工作区+权限+开关，实测 plan 模式下模型会先自查再给方案）；`CHAT_SYSTEM` 追加「权限与工作区」章节；`run_chat` 注入 `#### 当前运行环境` 块（工作区根、模式说明、三个确认开关、待确认数），步数上限改由 `settings["max_steps"]` 决定（`max_steps=None` 时读设置）。
- 路由（server.py）：GET `/api/builder/settings`、`/api/builder/approvals`；POST `/api/builder/settings/save`、`/api/builder/approval/{approve,reject,clear}`。
- 前端：左栏第三个页签「设置」（工作区路径 + 应用/恢复按钮 + 权限下拉（带说明）+ 4 个确认/备份开关 + 步数）；输入区上方「待确认操作」区（风险标签、内容预览、批准/拒绝，处理中禁用按钮）；工具卡片新增 `⏳ 待确认` 状态与文案（`.bc-tool.pending`）。
- 验证：本地单测 12 项（4 模式 × 操作类型闸门、队列增删、审批执行/拒绝、自定义工作区读写/黑名单/越界、工具清单 16 个）；部署版真实 LLM 端到端 4 步全通：plan 模式拒绝写入并给方案（文件未创建）→ default 模式进待确认（文件未落盘）→ 批准后真正写入 + 会话回写通知 → 拒绝后不落盘 + 回写拒绝通知。
- 清理：部署版测试文件（`plugins/_perm_e2e*.txt`）、测试会话、`builder_approvals.json`、`builder_history.jsonl` 已删；设置恢复默认（mode=default、workspace=""）。备份目录保留一份 17:43 的真实备份 `bridge/builder_api.py.20260918_174326_479530`。
- 提交 `e4cca55`（5 文件，+709/-52），推送 `98c0178..e4cca55`；已同步部署版并重启（PID 15092）。

## 构建助手新增「完全访问」档位 + 批准记忆（提交 0815762）
- 用户：「增加完全访问档位，选择完全访问时减少询问频率」。
- 权限档位 4 → 5：`PERM_MODES = (plan, default, acceptEdits, full, bypassPermissions)`。
  - **full（完全访问）** = 普通文件读写（含新建 / 覆盖改写）全自动；只有 `kind in ("sensitive","install")`（核心代码 / 智能体插件落盘）才询问。定位在 acceptEdits（高危都问）与 bypassPermissions（都不问）之间。
  - `classify_operation` 新增 `kind` 字段：`create` / `overwrite` / `sensitive` / `install`，供 full 档位做更细的询问判定（`level` 语义不变，仍供 acceptEdits 用）。
- **批准记忆**（减少询问频率的核心）：新增 `data/builder_rules.json` + `RULE_SCOPES=(file,dir,all)`。
  - `_rule_target(tool,args,scope)` 把一次批准归纳成规则；`_match_rule(tool,args)` 命中即跳过询问，并在 `run_chat` 给工具结果打 `auto_approved` 标记（模型与 UI 都能看到「按已记住的批准自动执行」）。
  - write_file 支持 `dir` 范围（`value` 以 `/` 结尾，命中用 `rel.startswith(v)`）；save_plugin/save_agent 按 name 匹配，无 name 时退化成 `*`。
  - 开关 `remember_approvals`（默认开）关闭后记忆不生效但仍保留；UI 可逐条删 / 全部清空。
  - `approve_approval_sync(bridge, aid, remember=False, scope="once")`；新增 `approve_all_sync` 一键批准全部待确认。
- 其他减少交互：完全访问档位下审批卡片的记住范围**默认选中「记住此文件」**（前端按 `bchat.mode === "full"` 判断）；待确认区多条目时显示「全部批准」；`run_chat` 环境块列出已记住的规则；`get_builder_settings` 返回 `remember_approvals` + 规则描述列表。
- 路由：GET `/api/builder/rules`；POST `/api/builder/approval/approve`（remember/scope）、`/approval/approve_all`、`/rules/delete`、`/rules/clear`。
- 验证：本地单测 12 组全过（完全访问对新建/覆盖放行、对照 acceptEdits 覆盖需确认、核心代码与落盘在 full 下仍询问、file/dir 级记忆命中范围正确、关闭开关后记忆失效、批量批准 2/2、记忆增删清空、plan 硬拦 / bypass 全放行）；部署版真实 LLM 端到端 4 步全通：① full 覆盖普通文件 → 0 待确认、内容改成 full-ok ② 新建 `bridge/_e2e_probe.py` → 待确认 kind=sensitive、未落盘 ③ 批准并记住(file) → 落盘 + 生成规则 ④ 再改同一文件 → `auto_approved`、0 询问、内容更新。
- 清理：部署版 `bridge/_e2e_probe.py`、`plugins/_e2e_exist.txt`、e2e 备份、会话 / 审批 / 规则 / 历史已删，设置恢复默认（mode=default）。
- 提交 `0815762`（5 文件，+357/-34），推送 `11322ee..0815762`；已同步部署版并重启（PID 31608）。

### 事故与恢复（重要教训）
- 本地单测脚本里写了 `bridge/builder_api.py` 作为「核心代码写入目标」，第 5 步 `approve_approval_sync` **真的执行了写入**，把 `bridge/builder_api.py` 覆盖成内容 `x`（1 字节）→ 随后 import 报 `NameError: name 'x'` 才发现。
- **恢复成功**：`write_workspace_sync(backup=True)` 在覆盖前已自动备份到 `data/builder_bak/bridge/builder_api.py.20260918_192825_302005`（99211 字节，含当时全部新改动），直接 `Copy-Item` 回原位即可，`py_compile` + 标记检查确认无损。
- **教训**：测试写操作时，目标路径必须是「不存在的临时文件」（如 `bridge/_tmp_probe_1.py` —— 既满足 `_sensitive_path` 判定又不会破坏真实源码），并在脚本开头加断言 `not os.path.isfile(target)` 拒绝覆盖已存在文件。这条已固化为跑写类测试的前置约定。

## 构建助手界面调整：设置项移到对话框下方 + 访问权限中文（提交 76b162f）
- 用户：「构建助手中设置里的工作区指定，访问权限，模型选择都放在对话框的下面，访问权限改为中文」。
- 位置调整（`webui/index.html`）：把 **模型 / 思考强度 / 访问权限 / 工作区** 四组控件集中到对话框输入区下方的新 `.bc-opts` 行（`flex-wrap` 自动换行；工作区那组 `flex: 1 1 320px` 占剩余宽度，含「应用 / 恢复默认」按钮）。原来模型与思考在页面顶栏 `head-actions`、工作区与权限在左栏「设置」页签 —— 均已移出。
  - 元素 id 全部保持不变（`bcModel`/`bcCustomModel`/`bcThink`/`bcPerm`/`bcWs`/`bcWsApply`/`bcWsReset`/`bcPermHint`/`bcWsHint`），JS 绑定无需改；`bcPermHint`/`bcWsHint` 移入新 `.bc-opt-hints` 行。
  - 左栏第三个页签 **「设置」→「高级」**，只剩：高危确认开关（4 个）、步数上限、保存按钮、已记住的批准列表。
- 权限中文（后端 `bridge/builder_api.py`）：新增 `_MODE_LABEL = {plan: 只读规划, default: 每次确认, acceptEdits: 自动应用, full: 完全访问, bypassPermissions: 完全放行}`；`get_settings()` 的 `modes` 每项增加 `label` 字段。前端下拉 `o.textContent = m.label || m.id`，**value 仍用英文 id**（后端判定依赖），下方 hint 仍显示 `_MODE_DESC` 中文说明。
- 权限切换从「change → 写 `#bcSettingsMsg`」改为「change → POST settings/save → 重新 `_bcLoadSettings()` → toast 中文名」，因为原提示元素已不在同一屏。
- 验证：py_compile / node --check 通过，49 个 `bc*` id 与 HTML 全部对应；部署版 HTTP 校验 —— `modes` 均带中文 label、`bc-opts`/`访问权限` 等中文串按 UTF-8 字节存在、`bcModel`/`bcPerm`/`bcWs` 的下标均在 `bc-composer` 之后（即位于对话框下方）。
- 注意：校验时发现部署版当前 `permission_mode` 已是 `full`（完全访问），非本轮改动所致（本轮不写设置），判断是用户自己选的 —— **保留用户设置，未重置**。
- 提交 `76b162f`（4 文件，+70/-28），推送 `906ce76..76b162f`；已同步部署版并重启（PID 30992）。
- 顺带记一条命令坑：PowerShell 里用 `python -c "…中文…"` 会因代码页把中文变成乱码导致 `SyntaxError`（`invalid character '\u20ac'`），校验中文串要写成临时 .py 文件再执行（与既有的「中文比较串乱码误报」同源）。

## 构建助手新增全套联网能力（提交 cf441e1）
- 用户：「在构建助手里增加全套联网搜索/抓取网页等联网功能」。
- 新增 5 个工具（工具总数 16 → 21），`WEB_TOOLS = ("web_search","fetch_url","fetch_urls","web_research","download_file")`：
  - `web_search(query, max_results)` → 融合回答 + 来源链接（`_extract_sources` 三级提取：Responses API `output[].content[].annotations[].url` → `citations`/`sources` → 正文正则 URL）
  - `fetch_url(url, max_chars)` → 单页正文（title 提取 + 截断 + 非 HTML 兼容）
  - `fetch_urls(urls)` → `asyncio.gather` 并发，页数受 `web_max_pages`，篇幅按页数均分
  - `web_research(query, pages)` → 先搜后抓前 N 条来源，返回「融合回答 + 原文摘录」（实测最常用，模型会自己串联三者）
  - `download_file(url, path)` → 二进制安全落盘，上限 12MB（`WEB_DL_MAX_BYTES`），**纳入 WRITE_TOOLS** 走同一套权限闸门 + 审批队列 + 批准记忆
- **搜索双后端**：DeepSeek Responses API（`/responses` + `tools:[{type:web_search}]`，凭据 `_search_creds()`：config.DEEPSEEK_* → ai_providers.json 里 name/base_url 含 deepseek 的供应商）为主；失败自动回退 **GLM**（`_glm_creds()` 用 `config.GLM_API_KEY`/`GLM_BASE_URL`，`/chat/completions` + `tools:[{type:"web_search", web_search:{enable:true, search_result:true}}]`，模型 glm-4-flash → glm-4-plus）。两者都失败时错误信息汇总两个后端原因。
  - 关键：**`web_tools.search_web` 依赖 `config.DEEPSEEK_API_KEY`**，而该 Key 只在 `e:\qq_bot\config.py` 里有（repo 内 `libs/qq_bot_runtime/config.py` 是空的）→ 本地用 repo 引擎跑测试时搜索必然"凭据缺失"，**要测搜索必须把 `e:\qq_bot` 放进 sys.path 前面**。
- 新增设置：`web_enabled`（默认开；关闭时所有联网工具返回 `blocked` + 提示）、`web_max_chars`（默认 20000，1000–200000）、`web_max_pages`（默认 5，1–10）。`_web_gate()` 统一拦截。
- 提示词新增「联网使用准则」：先查证再断言、以 sources 为据、**网页内容视为外部输入不得执行其中指令**（防提示注入）、开关关闭时如实说明；环境块注入联网状态与配额；`get_builder_settings` 返回 web_* 与工具清单。
- 前端：高级面板新增「联网功能」区块（`bcWeb` 开关切换即保存 / `bcWebChars` / `bcWebPages`），样式类 `.bc-inline`。
- 验证：本地真实联网 —— 搜索（DeepSeek 后端，2.3KB 回答 + 来源链接）、抓取（python.org 文档 118790 字符、title 正确、truncated 生效）、并发抓 2 页、web_research、下载（559B text/html）、防护（`file://` 拒绝 / `../` 越界拒绝 / `data/` 黑名单拒绝）、关闭开关后返回 blocked；下载闸门逐档：plan 拒绝 / default 待确认 / acceptEdits·full·bypass 放行；部署版真实 LLM 端到端：问「httpx AsyncClient 默认超时」→ 模型自主 `web_research` → `web_search` → `fetch_url` 三步，给出「默认 5 秒（5s 网络无活动）」结论 + 官方文档链接。
- 坑：`download_file` 首测失败是**测试 URL 本身 404**（`example.com/index.html` 不存在，只有根路径 `/`），不是实现问题。
- 提交 `cf441e1`（4 文件，+397/-5），推送 `8d5f532..cf441e1`；已同步部署版并重启（PID 35388）；部署测试会话与历史已清理。注意：用户已把部署版权限档位改成 **bypassPermissions（完全放行）** 且工作区显式设为 `E:\feiyu_app` —— 属用户设置，未改动。

## 构建助手展示思维链（提交 a855966）
- 用户：「把思维链显示出来，在当轮思考时显示，本轮思考结束后要折叠，点箭头展开」。
- **引擎层改动（重要，属引擎不是 App 层）**：`libs/qq_bot_runtime/ai_provider.py` 的 `chat()` / `_call_once()` / `_call_anthropic()` 新增 `keep_reasoning: bool = False`（默认 False → 对 bot 主链路零回归）。
  - 原代码对 OpenAI 兼容路径是 `msg.pop("reasoning_content", None)` **直接丢弃**；现在 `keep_reasoning=True` 时存到 `msg["_reasoning"]`；Anthropic 路径则拼接 `thinking` / `redacted_thinking` 块。
  - `_reasoning` 是**本地元数据键**，回传对话历史给 API 前必须剔除 → 构建助手用 `_strip_meta()` 过滤所有下划线开头的键。
  - 该文件必须同步 `e:\qq_bot\ai_provider.py`（引擎副本，备份在 `e:\qq_bot\_backup_20260918\`），否则部署报 `TypeError: chat() got an unexpected keyword argument 'keep_reasoning'`（本次已踩到）。
- `bridge/builder_api.py`：
  - `_chat_with_tools(..., keep_reasoning=True)` + `_strip_meta()`（发送前剔除本地键）。
  - `run_chat` 新增 **有序展示块 `blocks`**（`{type:"think"|"tool", round, ...}`，按发生顺序）+ `thinkings` 汇总 + `has_reasoning` 标记；思维链按轮次随历史持久化到消息的 `_reasoning`（单段上限 `REASONING_MAX = 12000` 字符）。
  - **修掉一个 off-by-one（我自己引入的）**：`base_idx` 必须取 `len(convo) - 1`（append 用户消息后的 len 指向下一条），否则 `turn = convo[base_idx:]` 会漏掉本轮第一条 assistant（含 tool_calls 的那条），会话历史出现 `[user, tool, assistant]` 这种缺帧结构。写回历史的正确做法：`base_idx` 用用户消息下标，`turn[0]` 替换为新的 user dict，其余按 `reasonings[base_idx + j]` 回填 `_reasoning`。
- 前端：新增 `_bcThinkCard(text, {live})` —— 思考中 `card.open = true` 且每秒刷新「💭 思考中… Ns（思考强度）」；结束时 `setText()` 填入真实内容并 **`card.open = false` 自动折叠** 成「💭 思考过程 · N 字 · 点此展开」，`<summary>` 前的 `▸` 用 CSS `rotate(90deg)` 表示展开态。发送时按 `r.blocks` 顺序渲染（think 已在占位卡片里合并渲染 → 跳过，其余交给 `_bcToolCard`）。历史回放（`_bcRenderMessages`）同样渲染 `m._reasoning` 的折叠块。模型未返回思考内容时移除占位并给出提示。
- **实测结论（重要）**：部署的 `deepseek-flash` **默认就会返回 `reasoning_content`**（非流式一次性返回），无需额外配置；`deepseek-reasoner` / `deepseek-v4-flash` 同样返回，`deepseek-chat` 不返回。`think=low/medium/high` 对该供应商**没有影响**（provider 未配 `think_param`/`think_body`/`extra_body`，`_build_payload` 不会加思考参数）。追问「1+1」这类过于简单的问题时模型可能**不产出** reasoning（返回为空），这属于模型行为，不是采集 bug。
- 局限：`stream: False` + 非流式 HTTP，所以「当轮思考时显示」实现为**展开的占位思考块 + 秒级计时**，真实内容在响应返回时一次填入并折叠；真正的逐 token 流式需要改引擎开 SSE（未做，风险高）。
- 验证：部署版端到端 —— ①「9.11 和 9.9 哪个大」→ `has_reasoning=true`、`thinkings=1`（338 字）② 带工具轮次 → `blocks=[think(r1), tool(r1)]` 顺序正确，会话文件为 `user → assistant(tool_calls=1, _reasoning=76字) → tool → assistant`，思维链已持久化 ③ 静态资源含 `_bcThinkCard` / `.bc-think` / `.bc-think[open] > summary::before`。
- 提交 `a855966`（4 文件，+164/-23），推送 `9a71a0b..a855966`；已同步部署版（`bridge/builder_api.py` + `webui/*`）与引擎副本（`ai_provider.py`）并重启（PID 9480）；测试会话与历史已清理。

## 构建助手：侧边栏收回/拉出 + 流式输出（提交 f3389d9）
- 用户：「构建助手下方侧边栏也增加收回/拉出功能，但收回时输入框留在底端。按上一个对话所说做流式输出，但增加开关供用户判断」。
- **侧边栏折叠**（纯前端）：左栏 `.bc-tabs` 尾部加 `#bcSideHide`（« 收回），`.bc-layout` 左上角常驻浮动 `#bcSideShow`（☰ 拉出，收起时才显示）；状态存 `localStorage['bc_side_collapsed']`，由 `_bcSide(collapsed)` 切 `#page-builder.bc-side-collapsed`。
  - CSS：`#page-builder.bc-side-collapsed .bc-side { display: none }` → 中栏 `.bc-main` 自动占满，**输入区在 `.bc-main` 内部底部，所以收回后输入框仍贴底**（已满足用户要求）；`.bc-chat` 收起时加 `padding-left: 96px` 避开浮动按钮。
- **流式输出**（可开关，默认开，localStorage `bc_stream`）：
  - 引擎 `ai_provider.py`：`chat()` / `_call_once()` 新增 `stream` / `on_delta`；新增 **`_call_openai_stream()`** 解析 `/chat/completions` 的 SSE —— 逐 delta 回调 `on_delta("think"|"content", 文本)`，聚合 `tool_calls` 增量（按 `index` 分槽、`arguments` 字符串拼接）、`usage` 统计与 telemetry，返回与非流式**完全一致**的消息结构（含 `_reasoning`）。**Anthropic 路径不做流式**（自动降级为非流式）。
  - `server.py`：新增 **POST `/api/builder/chat/stream`**，响应 `Transfer-Encoding: chunked` 手写分块（`b"%x\r\n" + payload + b"\r\n"`，结束 `b"0\r\n\r\n"`），先发 `: open` 心跳；客户端断开时置 `closed` 静默停写。`protocol_version = "HTTP/1.1"`，**必须自己写 chunked 帧**（SSE 无 Content-Length 时浏览器会缓冲）。
  - `builder_api.py`：`run_chat(..., stream=False, emit=None)` 推 `delta`(kind=think/content) / `think_end` / `tool_start` / `tool_end` / `done`(payload=完整返回体) / `error` / `notice`；`run_chat_sync(bridge, body, emit)` 为流式入口；供应商不支持时 `streamed=false`，端点补发 `notice` 让界面提示降级。
  - 前端：`_bcChatStream(payload, think)` 用 `fetch` + `resp.body.getReader()` 逐帧解析（按 `\n\n` 切分、只处理 `data:` 前缀）；思维链增量写入展开中的思考卡片（`.append()` + 字数与滚动跟随），正文增量实时写入气泡；`think_end` 折叠、`tool_end` 渲染工具卡片、`done` 收敛。**中断保护**：若已渲染过增量则抛 `err.__partial = true`，`_bcSend` 不再降级为非流式（避免重复渲染）；完全没产出时才降级为普通 POST。
  - `think` 卡片新增 `append()` 方法；`.bc-opt` 行里加了「流式输出」复选框（挨着「参考历史成功案例」）。
- 验证（部署版真实流式）：① 「9.11 和 9.9 哪个大」→ 17 个 content 增量帧，首个增量 **0.67s** 到达（证明边生成边下发，总耗时 0.8s），`done.streamed=true` ② 「列 plugins 子目录后总结」→ **120 个 think 增量帧（411 字）** → `think_end(110字)` → `tool_start/tool_end list_dir` → 第二轮 think（301 字）→ done，`blocks=[think,tool,think]`，事件顺序完全正确。
- 提交 `f3389d9`（6 文件，+375/-34），推送 `84877bd..f3389d9`；已同步部署版（`bridge/*`、`server.py`、`webui/*`）与**引擎副本 `e:\qq_bot\ai_provider.py`**（备份 `_backup_20260918\ai_provider.py.stream.bak`）并重启（PID 16584）；测试会话与历史已清理。
- 工具环境备注（**已被下一条更正**）：`agent-browser` 当时未安装（`agent-browser` 命令不存在），本机无法做自动化浏览器截图验证；改用「HTTP 级流式时序验证 + 静态资源断言」替代。

## 视觉验证（真实浏览器截图）+ 文案微调（提交 e43c623）
- 用户：「继续」→ 承接上一条「agent-browser 未安装、无法做视觉回归」的遗留项，本次把浏览器自动化环境配通并做了完整界面视觉验证。
- **装通 agent-browser（两个坑）**：
  1. `npm install -g agent-browser` 首次失败：npm 缓存目录被配到 **`F:\qq_bot\caches\npm-cache`，而本机没有 F 盘**（与既有「本地工具路径漂移」同源）→ 报 `mkdir '\\?'` ENOENT。解法：`npm install -g agent-browser --cache E:\npm-cache`（并设 `$env:npm_config_cache`）。已建 `E:\npm-cache` 常驻备用。
  2. **不要走 `agent-browser install`**（要下 ~500MB Chromium）。改用 **`agent-browser connect 9222`** 复用系统 Edge：先 `Start-Process msedge.exe -ArgumentList '--headless=new','--disable-gpu','--no-first-run','--remote-debugging-port=9222',"--user-data-dir=E:\edge-cdp-profile",'--window-size=1560,980'`（Edge 在 `C:\Program Files (x86)\Microsoft\Edge\Application\msedge.exe`），确认 `http://127.0.0.1:9222/json/version` 可用后 connect，之后 `open/click/fill/press/screenshot` 均可正常驱动。
  - **坑：CLI 传中文参数会被 GBK 破坏**（`find text "构建助手"` → `鏋勫缓鍔╂墜`）。解法：**用 CSS 选择器代替中文**，如 `agent-browser click 'a[data-page="builder"]'`、`click '#bcSideHide'`、`click '.bc-think > summary'`；填输入框用英文提示词。
- **验证结果（截图逐条确认，全部通过）**：
  - 构建页三栏渲染正常：左栏文件树（目录/文件/大小/＋参考）、中栏对话 + 输入区、选项行（模型/思考/访问权限=完全放行/工作区）位于对话框下方，与 `76b162f` 目标一致。
  - **侧栏收回**：点 `#bcSideHide` → 左栏隐藏、左上角出现「☰ 拉出」浮动按钮、中栏占满全宽，**输入框与选项行仍紧贴底端**（用户核心要求达成）；点 `#bcSideShow` 恢复正常（`is visible '#bcSideHide'` → true）。
  - **流式实时渲染**：发送后 3s 截图即见「💭 思考中… 3s（快速）」展开卡片 + `✓ list_dir dir=plugins` 工具卡片（工具在轮次中就已渲染）；状态行「进行中 3s …」。
  - **思考链自动折叠 + 点箭头展开**：本轮结束截图显示 `▸ 💭 思考过程  1 轮 · 523 字 · 点此展开`（已折叠）；点击 `.bc-think > summary` 后展开显示完整推理原文（含 "The user wants me to list the plugins directory subdirectory names … Let me re-list to confirm."），与最终答案自洽；状态行「完成（3s，1 个工具调用，思考 1 段，流式）」。
  - **状态持久化**：在界面里关掉「流式输出」开关 + 收起侧栏 → 刷新页面 → 重新进入构建页 → `is checked '#bcStream'` = **false**、`is visible '#bcSideShow'` = **true**（两个 localStorage 状态都正确保持）。
  - **非流式路径**：关闭流式后发消息，状态行显示「完成（1s，0 个工具调用）」无「，流式」后缀，回答正常渲染 → 开关确实切换了传输路径。
- **顺带修文案**：未返回思考内容时原提示断言"当前模型/供应商未开启思维链"，但实测 deepseek-flash 是支持的、只是简单问题不产出推理 → 改为「可能是本轮问题较简单、模型未产生推理，或该模型/供应商不支持思维链」（`webui/app.js`）。
- 清理：仓库内 `_shot_*.png`（7 张验证截图）、部署版 `data/builder_chat/*` 与 `builder_history.jsonl`、`E:\edge-cdp-profile`（42MB 临时 profile）均已删除；无头 Edge 进程已结束。
- 提交 `e43c623`（1 文件，+3/-4），推送 `5b8399c..e43c623`；已同步部署版 `webui/app.js` 并重启（PID 22632）。
- 备注：`E:\npm-cache`（npm 缓存）与已全局安装的 `agent-browser@0.38.1` **保留**，后续做视觉回归可直接 `agent-browser connect 9222` 复用系统 Edge。

## 落地「构建助手自评的三条协议改进」（提交 84527b3）
- 用户发来 3 张截图（`C:\Users\21907\Videos\Captures\肥鱼娘 · App 控制台 2026_9_18 21_10_{00,15,22}.png`），是**用户用构建助手自己做的「插件协议开发可用性评估」**：3 个会话 `copy_of_codebuddy` / `trea` / `codebuddy` 从不同角度得出「1 真实缺口（无官方 UI 事件通道，插件只能 hack 内部对象）+ 3 处文档漂移 + 6 条建议 + 2 项已知非缺陷」。用户只说「看看里面的建议」→ 我先逐条**核实真伪**，再把站得住的缺口落地。
- **核实结论（重要：不能照单全收）**：
  - ✓ 属实：**无官方 UI 通道**（`core` 上没有 `app_bridge`，插件要往 UI 推消息只能 `sys.modules` 取内部对象）；`plugins/greeting_demo/plugin.py` 正是用 `from feiyu_app.bridge.ref import get_bridge` 这种 hack（且实际拿不到）；`PLUGINS.md` 写了 `pkg_requires` 但**实现里没有**（文档漂移）；manifest 无校验（`read_manifest` 静默返回 None，坏包「凭空消失」）；sidecar stdout/stderr 丢 DEVNULL（崩溃零线索）；`repr(e)` 套在 f-string 里（仅日志可读性）。
  - ✗ 不实：截图里说「版本号是占位符待填」——实际 `version` 在 `manifest.get("version", "1.0.0")` 已正确读取，属误判。
- **本次落地 6 项**：
  1. **官方 UI 事件通道 `core.app_bridge`**（`bridge/core_bridge.py`）：`build_engine()` 构建 core 后立即 `core.app_bridge = self`（**注入点必须在 core 构建后、start 前**，且 try/except 包裹），插件用 `getattr(core, "app_bridge", None)` + `bridge.push(session, ev)` 推 message/image/audio/tts/status/error；`push` 线程安全、`session=None` 为全局广播。
  2. **manifest 装载前静态校验**（`bridge/pkg_manager.py`）：新增 `validate_manifest(meta, pkg_dir) -> (errors, warns)` + `load_manifest(pkg_dir) -> (meta, errors, warns)`；覆盖必填字段 / `kind ∈ VALID_KINDS=("platform","feature","brain","sidecar","local")` / **name 与目录名一致** / 依赖字段类型 / `sidecar.script` / `config_schema[].key` / `MANIFEST_SCHEMA_VERSION=2`（缺失按 1 告警放行，高于本机则拒绝装载）；错误全部是**人话**。
  3. **坏包不再静默消失**：manifest 无效的包以 `kind="invalid"` + `manifest_errors` 出现在列表里（便于排障）。**坑：`scan_packages` 的 `name` 键必须用目录名 `fn`**，否则「缺 name」的包会 KeyError（我第一版就踩了，测试直接抓到）。
  4. **依赖补齐**：实现 `pkg_requires`（依赖其它插件包，装载前校验存在且启用）；`optional_requires` 改为**软依赖**（缺失只进装载结果的 `warnings`，不阻塞）。
  5. **模块残留清理**：新增 `purge_wrapper_modules(name)`，在 `_load_wrapper`（重装/热重载前）、`unload` 成功与**装载失败**三条路径调用，清掉 `sys.modules` 里 `feiyu_pkg_<name>` 及其子模块 —— 修复「同名重装沿用旧模块对象、改了 plugin.py 行为不变」。
  6. **sidecar 可排障**（`bridge/sidecar_runner.py`）：stdout/stderr 合并落盘 `<qq_bot>/logs/sidecar_<name>.log`；新增 `wait_ready(timeout=15)` 轮询探测 `host:port`（`port=0` 视为无需探测；进程中途退出记录 `returncode`）；`status()` 增 `log`/`exit_code`；`tail_log(n)`；`stop()` 里 `_close_log()` 关句柄。
  7. **构建助手可读协议文档**（`bridge/builder_api.py`）：`CONTEXT_ALLOW_DIRS` 之外新增 `CONTEXT_ALLOW_FILES = (README.md, OVERVIEW.md, PLUGINS.md, PLUGIN_PROTOCOL.md, requirements.txt)`，修掉截图里「README 不在允许根内，协议部分未读到」；**坑：`os.path.normcase` 在 Windows 会小写化路径**，比对文件名必须用 `.lower()` 集合（`_CONTEXT_ALLOW_FILES_LC`），否则 `plugins.md` 永远匹配不到 `PLUGINS.md`。
  8. **示例包重写并纳入仓库**（`plugins/greeting_demo/{manifest.json,plugin.py}`）：自包含（不 import 任何 qq_bot 模块），带 `schema_version:2`、`config_schema`（间隔/文案）、`on_config` 热生效，示范官方通道用法；`plugins/` 在 `.gitignore` 里是**按子目录忽略、`!plugins/greeting_demo/` 已放行**，故该包进版本控制（主体插件包仍在部署版）。
- **文档同步**：`PLUGINS.md` 新增 **4.5 节「向 App 界面推事件（官方通道 core.app_bridge）」**（含事件类型表 + 反例警示）；manifest 字段表补 `schema_version`/`pkg_requires`/`plugin`/`builtin`/`default_on`/`config_schema`，并写明**校验规则与 error/warning 语义**；sidecar 章节补日志与就绪语义。
- **验证**：
  - 本地单测：8 类问题包全部被正确判级（kind 非法 / 缺 name / name 与目录不符 / sidecar 缺 script / 依赖字段类型错 / 未来 schema_version / 合法包 / 包依赖缺失），装载拦截生效（`_t_*`→`zt_*` 后扫描可见），依赖校验、模块清理、sidecar 探测与日志 API 均通过；构建助手可读 `PLUGINS.md`（含 `app_bridge` 关键词）且越界仍拒绝。
  - **部署版端到端（官方通道打通）**：装载 `greeting_demo` → `POST /api/plugins/greeting_demo/config` 设 `interval_seconds=10` 热生效 → 10 秒后事件流收到 `push(None, {...})` 的问候消息（global 广播），确认「插件 → App UI」链路可用。
- **踩到的其它坑**：
  - `scan_packages` **跳过 `_` 开头的目录**（与 `_WORKSPACE_DENY`/隐藏目录规则一致）→ 测试包名不能用 `_` 前缀，改用 `zt_*`。
  - `PLUGINS.md` 里提到的 `README.md` 在**部署版 E:/feiyu_app 里不存在**（只有仓库有 README/OVERVIEW/PLUGINS），已在日志里说明，未强行创建。
  - 命令行一度整体失灵（连 `Set-Content` 都不生效）→ 后续统一改成「写 `.py`/`.ps1` 脚本再执行」，比内联 `-Command` 稳（cmd 里嵌套引号必被转义破坏）。
- 提交 `84527b3`（7 文件，+492/-21），推送 `49b6324..84527b3`；已同步部署版（备份 `data/_sync_bak_proto_20260918_*/`）并重启（PID 41040），部署版 7 文件与仓库 md5 全一致；示例插件配置已恢复默认，临时脚本/截图/Edge profile 均已清理。

## 工作区支持从硬盘选择目录（提交 00ab936）
- 用户：「工作区增加从硬盘里选择的功能，手输地址太麻烦了」。
- **后端**（`bridge/builder_api.py`）：新增 `list_disk_dirs(path)` + `list_disk_dirs_sync` + 路由 `GET /api/fs/dirs?path=`。
  - `path` 空 → 盘符（Windows 扫 `A:`–`Z:` 存在者）+ 常用位置（`_QUICK_DIRS`：用户目录/桌面/文档/下载 + App 目录）+ `current_workspace`；否则 → 该目录子目录（`os.scandir`，按名排序，带 `parent`）。
  - **只列目录名、不返回文件、不读内容、不写** —— 浏览是纯只读操作，安全面最小；权限受限（PermissionError）与不存在路径都返回人话错误；入参做 `strip('"')` 容错。
- **原生对话框**（`bridge/app_window.py`）：js_api 新增 `pick_folder(initial)`，用 pywebview `FOLDER_DIALOG` 弹系统「选择文件夹」；返回 `{supported, path}`：`supported=False` = 该模式无原生能力（Edge/浏览器），`path=None` = 用户取消（前端不再弹内置选择器）；初始目录非法则不透传 `directory` 参数。
  - **修掉一个长期潜伏的 bug**：`import webview` 在 `try_app_window()` 内是**函数局部名**，js_api 里引用 `webview` 必然 `NameError` 并被 `except` 静默吞掉 —— 意味着 `set_title` 改标题栏**一直是失效的**（此前记忆里写的「js_api.set_title 实时改标题栏」与事实不符）。现改为模块级 `_webview`（导入成功时赋值），`set_title` 与 `pick_folder` 都正常。
  - `js_api=_AppearanceApi()` 在 `create_window` 时传入（第 145 行），故实例方法自动可前端调用。
- **前端**（`webui/*`）：
  - 工作区输入旁新增「📁 浏览…」`#bcWsBrowse`：先试 `window.pywebview && window.pywebview.api.pick_folder(cur)`（真实 App 窗口 = 系统对话框），`supported=False` 或异常则 `_bcPickOpen()` 开内置选择器。
  - 内置选择器 `#pickModal`（复用 `.modal` 结构）：`#pickUp` 上一级（盘符根 `E:` → `E:\` 特殊处理）/ `#pickPath` 回车跳转 / `#pickQuick` 常用位置按钮 / `#pickList` 目录行（当前工作区标 ✓）/ `#pickChoose` 选择此目录；点遮罩或 ✕ 关闭。
  - `_bcApplyWorkspace(path)` 统一「写 `#bcWs` → POST settings/save → 重载设置 → 文件树回根 → toast」；`_bcSaveSettings` 在 `changed` 含 `workspace` 时也回根（切工作区后旧相对路径可能不存在）。
- **验证**：
  - 本地单测：盘符（C:\, D:\, E:\）、常用位置、不存在路径、引号容错、文件路径当目录、父级拼接算法；**原生对话框 6 例**（成功 / 取消 / 不支持 / 异常回退 / 非法初始目录不透传 / `set_title` 修复后可用）全通过。
  - **浏览器端到端**（无头 Edge 走内置兜底路径）：点「浏览…」→ 弹窗出现（3 个盘符行）→ 点行进入 `C:\` → 路径框跳转 `E:\feiyu_app`（提示「当前浏览：E:\feiyu_app」）→ 点「选择此目录」→ `#bcWs` = `E:\feiyu_app`、弹窗关闭、提示更新为「当前生效工作区：E:\feiyu_app（应用目录）」。
  - 部署版静态资源（`_bcBrowseWorkspace` / `pick_folder` / `bcWsBrowse` / `pickModal` / `.pick-row`）与接口全通过；工作区已恢复默认（空=应用目录）。
- **重要工具坑（曾导致验证无效，务必记住）**：
  - `agent-browser` 的调用**不能**用 PowerShell「函数 + 数组 splat」（`function AB($a){ & agent-browser @a }` 形式）—— 实测会把命令变成非法参数、**只打印 help 文本**，于是 `is visible` 之类断言拿到空串，看起来「命令不存在」。**正确姿势**：直接一行一个命令（`agent-browser click '#sel'`），并可用 `agent-browser eval "<js>"` 做精确断言（本次用它读 `#pickPath` value、`#pickModal` class、`#bcWs` value 最可靠）。
  - `agent-browser` 子命令确认存在：`get <text|html|value|count|box|...>`、`is <visible|enabled|checked>`、`find`、`eval`、`screenshot`、`snapshot`（element ref 形如 `@e1`）。
  - `cmd /c "powershell -Command ""…""` 这种嵌套引号会坏（报 `'Out-Null' is not recognized`）→ 一律**写 `.ps1`/`.py` 脚本再执行**，输出重定向到日志文件后 grep。
- 清理：临时脚本/截图/日志 15 个文件、`E:\edge-cdp-profile2|3` 两个 profile、无头 Edge 进程（9 个）全部删除/结束。
- 提交 `00ab936`（6 文件，+277/-4），推送 `b989a21..00ab936`；已同步部署版并重启（PID 32088）。

## 更新 README 与速览（提交 53c43ab）
- 用户：「更新readme和速览」→ 把本轮所有新能力写进 `README.md` 与 `OVERVIEW.md`。README 393 行、OVERVIEW 25 行（保持单页规模）。
- **README**：
  - 新增 **`### 构建助手（对话式 Agent）`**（位于「插件系统」与「外观」之间）：三栏 ASCII 界面示意、左栏折叠（收起后输入框仍贴底）、21 个工具的工具循环（上限 14 步可在「高级」调）、**5 档访问权限表**、待确认队列与「记住批准」、工作区从硬盘选择（原生对话框 + 内置浏览器）、联网 5 件套（含 12MB 上限与 DeepSeek→GLM 双后端）、思维链与流式、会话/历史/备份路径、安全约定（防提示注入）。
  - **修正文档结构错误**：原文件里 `### 外观` 与 `### 模型注册表（LLM 供应商）` 被误嵌在 `## 插件协议` 标题之下（TOC 却把它们列在「功能模块」下，文档与目录自相矛盾）→ 现把 `## 插件协议（第三方接入规范）` **移到模型注册表之后**独立成节，并前置 5 条要点（`core.app_bridge` 官方通道 / manifest 校验 / 依赖硬软 / sidecar 日志与就绪 / 参考实现 `plugins/greeting_demo/`）。
  - TOC 补 `构建助手（对话式 Agent）` 与 `模型注册表（LLM 供应商）` 锚点；仓库结构补 `bridge/builder_api.py`（职责说明）与 `plugins/greeting_demo/`；侧栏清单**改为实际顺序**「仪表盘 / 聊天 / 记忆 / 总结 / 插件 / 配置 / 构建助手 / 外观」并注明「AI 供应商（模型管理）」是**配置页内的面板**（原稿误列为独立导航项）。
  - 常见问题新增 4 条：权限档位导致「一直要确认 / 改不动文件」、工具调用需模型支持 tools、「联网搜索失败看开关与密钥」、改 `webui/` 后仍显示旧界面（no-store + 重启）。
- **OVERVIEW（速览）**：插件系统条目补 manifest 校验与依赖硬/软；新增「构建助手（对话式 Agent）」与「插件协议」两个条目；仍保持一页（25 行）。
- **文档声明与代码逐条核对（写脚本校验，避免文档吹牛）**：工具数 **21** ✓、`CHAT_MAX_STEPS=14` ✓、下载上限 **12MB** ✓、权限档位与中文名（只读规划/每次确认/自动应用/完全访问/完全放行）✓、`MANIFEST_SCHEMA_VERSION=2` ✓、15 个前端关键元素 id 全部存在 ✓、`plugins/greeting_demo/plugin.py` 存在 ✓。
- **部署版文档同步**：`README.md`/`PLUGINS.md` 部署版本就存在（同一份），**`OVERVIEW.md` 部署版原本缺失 → 本次新建**（更正上一条记忆里「部署版没有 README」的误记：README 是有的，缺的只是 OVERVIEW）。
- 提交 `53c43ab`（2 文件，+73/-9），推送 `022d158..53c43ab`。
