# 2026-09-14

## 项目探索：读取项目文件、了解整体架构
- 项目 = 「肥鱼娘 App 独立便携版」(`e:/feiyu_standalone`)：AI 智能体伴侣桌面应用，全便携自包含（自带 Python 3.13 解释器、venv 依赖、VoxCPM2 TTS 模型），拷到任意 Win10/11 即可运行。
- 启动链路：`FeiyuApp.exe`/`start_app.bat` → `app.py`（引导：定位 `libs/qq_bot_runtime` → 应用覆盖层 `data/app_settings.json` → 重建 AI 供应商路由 → 补 telemetry 补丁）→ `LoopThread`(asyncio) → `CoreBridge` → `server.py`(HTTP, 127.0.0.1:8900, Windows 独占绑定防多实例) → pywebview/Edge/浏览器三级降级桌面窗口。
- 核心设计：**零源码改动复用 qq_bot 引擎**（`libs/qq_bot_runtime/` ~17GB，agent_core/chat_service/记忆系统/多 AI 供应商/多大脑）。
- `bridge/` 桥接层：core_bridge(生命周期+聊天+SSE 事件中枢+AppReplyTarget/表情包解析)、loop.py(独立 asyncio 线程)、pkg_manager(插件包管理)、memory_api/summary_api/config_api/plugins_api、app_window。
- `settings_store.py` 覆盖式配置中心：不写引擎 config.py，所有界面改动落 `data/app_settings.json`，启动/保存时 setattr 到 config 模块热生效；CONFIG_SCHEMA 同时是配置页 UI 数据源；供应商键改动触发 `ai_provider.reload_provider_config()`。
- `webui/` 前端 3 文件：index.html 六页（仪表盘/聊天/记忆/总结/插件/配置）+ app.js + styles.css，SSE(`/api/events`) 实时事件流，事件带自增 id 去重。
- `plugins/` 16 个可插拔包：manifest.json(name/title/kind/switch/plugin/requires) + plugin.py 包装器（create_plugin/create_brain）；4 种 kind：platform/feature/brain/sidecar(sidecar 为子进程 script/host/port)；`groups.json` 依赖分组(core/voice/qq/bilibili/mc/desktop/services)；启用状态存覆盖层 `pkg_plugins`；核心构建期 attach_to 预注册避免与内建注册表双注册。
- sidecar 包：sidecar_memory/sidecar_monitor/sidecar_telemetry（子进程独立服务）。

## venv 自举机制调查（用户问能否删 venv、首启能否自动配环境）
- `_launcher.py`（FeiyuApp.exe）**专为无 venv 包设计**：`ensure_pyvenv()` 用包内 `runtime/python/python.exe`（51MB）现场 `python -m venv` 创建 venv；`setup_deps()` 三级自举：`libs/offline_deps_core.zip` 离线解压（2-3 分钟零网络）→ 无 zip 则 pip 按 `requirements-portable.txt` 在线装（清华源+PyTorch cu128 源，30-90 分钟）→ 失败弹窗。
- **关键陷阱**：删 venv 必须同时删 `libs/qq_bot_runtime/.setup_core_complete` 标记（setup_deps 见标记即 return True，空 venv 会直接启动导致 import 全挂）。完成判定用代表包探测（torch/httpx/webview 任一缺失即重装），不用"site-packages 非空"。
- `start_app.bat` **不支持 venv 缺失**（直接报错退出），只做 pyvenv.cfg 重写；需先用 exe 启动一次建好 venv。
- `libs/` 下已有 `offline_deps_core.zip` 和 `offline_deps_vox.zip`；vox 同理可选（venv_vox + setup_vox.bat / vox_tts 插件降级 GLM 云端）。
- exe 首启静默（show_progress=False），解压 2-3 分钟窗口无提示，之后每次启动靠标记秒过。

## venv 瘦身执行（2026-09-14 完成）
- 已删除：`venv/`（5.45GB）、`venv_vox/`（~5GB）、`.setup_core_complete` 标记。
- 已保留：`runtime/python/`（基础解释器）、`offline_deps_core.zip`、`offline_deps_vox.zip`。
- E 盘删除后剩 15GB 空闲。下次首启：exe 现场建 venv → 解压离线包 → 写新标记，vox 同理可选。
- 注意：PowerShell 命令含中文字符串会因编码（UTF-8→GBK）解析报错，命令一律用纯英文。
- 源项目 E:\qq_bot 的 venv 有 4 个 python 进程在跑，与本项目无关，别误杀；IDE 自身 python 属 .workbuddy。

## 首启进度窗口 + vox 静默后台（2026-09-14 实现并实测通过）
- 新增 `_progress_gui.py`：纯 ctypes Win32 进度窗口（零第三方依赖；基础解释器和打包源环境都无 tkinter，故不用 tkinter）。venv/pip 黑盒阶段伪进度爬行（封顶 92%），unpack 阶段真实字节进度+线性 ETA，可取消（窗口 X 同取消，解压循环感知，半残由代表包探测自愈）。
- `_launcher.py` 重写：`ensure_pyvenv(rt, name)` / `setup_deps(..., venv_name, progress, cancel)` 泛化；`spawn_app()` 抽公共；快速路径（venv+标记在→秒启不开窗）；标记自愈（标记在但代表包缺→删标记重装）；GUI 异常兜底静默直跑。
- 新增 `_vox_setup.py`：vox 分离子进程（DETACHED_PROCESS，runtime python 跑），静默创建 venv_vox→解压/pip→写标记；失败写 `.setup_vox_failed`，24h 内不重试；日志 `data/vox_setup.log`。launcher 每次启动都拉起（内部自判秒退）。
- `setup_deps` 完成判定改为代表包探测优先、标记仅缓存。
- 用 `E:\qq_bot\venv` 的 PyInstaller 6.22.2 重打包：`& E:\qq_bot\venv\Scripts\python.exe -m PyInstaller --noconfirm --clean --distpath E:\feiyu_standalone --workpath E:\feiyu_standalone\_build E:\feiyu_standalone\_build\FeiyuApp.spec`；新 exe 8.01MB。
- 实测：首启约 105 秒完成（venv+core 解压+vox 并行），App 自动拉起 8900 OK；exe 快速路径秒启 OK。测试后进程已清理，环境处于"已配置完成"状态。

## 第二次删除 venv + 回收站空间排查（2026-09-14）
- 应用户要求再次删除 venv/venv_vox/标记以重新体验首启。发现**环境的安全删除护栏会把 Remove-Item 的删除转入回收站**（首轮删除实际是永久删除、第二轮进了 E 盘回收站占 20.6GB，两次行为不一致）；护栏有时报 SAFE_DELETE_BULK_GUARD_ERROR（合并两条 Remove-Item 触发），单条命令重试即可。
- E 盘回收站 71 项全部来自本项目（feiyu_standalone + feiyu_rescue 历史构建产物），用 `cmd /c rd /s /q` 直接删 SID 文件夹（Clear-RecycleBin -DriveLetter E 报"找不到文件"怪癖错误不可用），释放后 E 盘 25.1GB 空闲。
- 教训：大体积删除后要核对 Get-PSDrive 空闲是否真涨了；没涨就查回收站。C/D 盘回收站有其他会话文件，绝不能整盘清。

## 空间压缩（2026-09-14，用户选定方案执行完毕）
- 空间账本：28.94GB = core zip 5.46 + vox zip 5.08（与 voice_pack/venv_vox 完全重复）+ voice_pack/models 4.62（VoxCPM2 权重）+ hf_cache 7.0（Qwen2.5-VL-3B 本地视频理解回退，云端优先不受影响）+ tools 0.68 + mc_bot/mod 0.65 + 其他。
- 已执行：①删 `libs/offline_deps_vox.zip`（省 5.08GB，cmd /c del 永久删防回收站截留）；②`_vox_setup.py` 重写为四级自举（venv_vox 就绪→秒退 / voice_pack/venv_vox 存在→junction 秒链接【主路径，vox_tts 插件的 _ensure_junctions 会跳过已存在】/ 旧 zip 兼容 / pip 兜底），首启 vox 环节从解压 5GB 变秒级；③NTFS LZX 压缩 voice_pack/venv_vox（compact /c /s /i /q /exe:lzx，5.45GB→3.23GB，压缩率 1.7:1，省 2.23GB）。
- E 盘空闲：25.1 → 32.3 GB（共省 7.31GB）。项目 28.94→21.7GB 左右。
- 未执行备选（用户未选）：删 hf_cache（7GB，本地视频理解回退模型）、删 MC 资源（0.65GB）、压缩 models（safetensors 已压缩无效）。
- core zip 必须保留（首启离线秒装的关键）。

## 分发分离：主包与插件资源分开下载（2026-09-14，用户核心诉求）
- 用户真实意图：主体核心与插件可分开下载（非本机省空间）。事实：plugins/ 16 包仅 0.1MB（薄包装器），插件大资源在引擎内（mc_bot 0.43 + mc_mod 0.22 + tools 0.68GB）。
- 已分离（同卷 Move-Item 瞬时 + mklink /J 原位回接，代码零改动）：`libs/mc_pack/`（mc_bot、mc_mod、_mc_ref）、`libs/tools_pack/`（tools）。junction：runtime/{mc_bot,mc_mod,_mc_ref,tools} → pack 实体。
- `_launcher.py` 加 `PACK_LINKS` + `ensure_packs(base, rt)`：每次启动幂等把 libs/ 下已有 pack junction 接回原位（毫秒级），新机器 pack 丢进 libs/ 即自动接线。缺 pack 优雅降级（vox→云端、MC 报依赖、语音转码缺失）。
- `_make_packs.py` 分发打包脚本：core（排除 packs/venv/hf_cache/运行数据/_build/dist，junction 用 st_file_attributes & REPARSE 检测跳过防重复打包）/voice/mc/tools/vl 各独立 zip → dist/；DEFLATED level=1。实测 mc：6981 文件 0.70GB→0.25GB 71s。
- voice_pack 模式已是先例（models/venv_vox junction）。vl_pack（hf_cache 7GB）做成可选分发但本机文件未动。
- README 已补「分开下载」章节（core ~8GB 必下，packs 可选）。
- 坑：exe 冒烟 7 秒等待不够（sidecar 多初始化久），15 秒才稳；Start-Job 里 spawn 的子进程会被 Remove-Job -Force 连带杀，冒烟要用 Start-Process 独立跑。
- 新机器安装流程：解压 feiyu_core.zip → pack zip 解到 libs/ 对应位置 → 双击 exe 自动接线。

## 恢复未首启动状态（2026-09-14，第三次清 venv）
- 用户在分发分离后要求恢复未启动状态。清除项：venv 实体（rd /s /q）、venv_vox/models junction（cmd rmdir 只删链接）、.setup_core_complete、app_stdout.log、vox_setup.log、memory/time_indexed/2190720017.db（32KB 首启初始化结构）。
- 保留项：mc/mod/_mc_ref/tools 四个 junction（分发结构成果）、mc_pack/tools_pack、voice_pack（models+venv_vox 实体）、runtime/python、offline_deps_core.zip、FeiyuApp.exe（14:06 新打包）、memory/reflection_memory.py（引擎文件非产物）。
- 用户中途自己跑过一次首启（vox sidecar 进程残留 21352 已停）。
- 坑：多条 cmd /c 用 PowerShell 分号串接时解析错乱（第四条参数被拼进前三条导致整批失败），cmd 类命令必须逐条执行；junction 用 cmd /c rmdir 删最安全（Remove-Item 可能触发安全删除护栏且语义不明）。
- E 盘空闲 32.1GB。当前 = 全新未启动包 + 分发分离结构。

## 插件代码包分离（2026-09-14，plugins -> libs/plugins）
- 用户要求"插件移出文件夹"：plugins/（16 包装器 0.1MB+groups.json）移到 libs/plugins，根目录 junction 回接（pkg_manager.PACKAGE_DIR=APP_DIR/plugins 与 groups.json 路径不变，代码零改动）。
- _launcher.py ensure_packs 加专用规则：libs/plugins 存在且根 plugins 不存在 → mklink（结构不同于 PACK_LINKS 的 libs/<pack>/<name>→rt/<name>，单独处理）。
- _make_packs.py 加 plugins 项（libs/plugins → plugins_pack.zip）；core.zip 中 plugins 是 junction 被 walk 剪枝自动排除，core 从此不含插件代码；vl_pack 安装位置修正为 libs/qq_bot_runtime/（hf_cache 在 runtime 下，README 已同步）。
- 验证：重打包 exe → 冒烟时走了完整首启（venv 已删）2-3 分钟后 /api/plugins 返回 16 包全可见 → 清理进程并再次恢复未启动状态（venv/junction/标记/日志全清，分发 junction 与资产保留）。
- 坑：PowerShell shell 会残留 cmd 上下文（Move-Item 报"不是内部或外部命令"），混合命令要拆开逐条；cmd & 串接时 safe-delete 对 junction rmdir 报 non-empty fail-closed（实体未损），单条 cmd /c rmdir 才是可靠删法；taskkill /im python* 会误伤 IDE/其他项目 python 进程，尽量用路径过滤的 Stop-Process。
- 最终状态：主包（core.zip）不含插件代码，plugins_pack.zip 独立分发；本机 = 未启动状态 + 全部 pack junction 接线完毕。

## 插件总仓迁移 E:\plugins（2026-09-14，最终形态）
- 用户要求"所有插件移到 E 盘 plugins 文件夹，只留最小核心/引擎"。E:\plugins 原为空目录。
- 最终结构：E:\plugins\{plugins(16包+groups.json), mc_pack(mc_bot/mc_mod/_mc_ref), tools_pack(tools), voice_pack(models/venv_vox), vl_pack(hf_cache)}。standalone/libs 只剩 qq_bot_runtime + offline_deps_core.zip。
- junction：根 plugins→E:\plugins\plugins；rt/{mc_bot,mc_mod,_mc_ref,tools,hf_cache}→E:\plugins\<pack>\...；models/venv_vox 由 ensure_packs 启动时自动建。
- **重大 bug 修复：os.path.join("E:", "plugins") 生成相对路径 "E:plugins\..."（splitdrive 不带反斜杠）→ isdir 永远 False → 包外接线全静默失败**。修复：_launcher._pack_candidates/ensure_packs + vox_tts/plugin.py _voice_pack_dir 三处 drive+"\\\\"。教训：splitdrive 结果做 join 必须补尾反斜杠；接线失败是静默的，必须靠日志/实测发现。
- 连锁事故：ensure_packs 失败后 vox_setup.py 在 rt/venv_vox 建了真 venv 并 pip 在线装 vox 依赖（30-90min 白装）——已杀进程删除，junction 接 voice_pack/venv_vox 即用环境。教训：vox_setup 的 ensure_pyvenv 会在 junction 缺失时真建 venv，分发场景必须先保证接线成功。
- ensure_packs 现含 vl_pack/hf_cache 接线；_make_packs.py PLUGINS_REPO=splitdrive(BASE)+"\\plugins"，各 zip 内部路径 <pack>/... 统一解压到 <盘>:\plugins\；core 排除清单加根 plugins。
- 验证：单测路径推导 ✓；重打包 exe → 冒烟 16 插件可见 + vox_tts running ✓ → 恢复未启动状态（venv/junction/标记/日志清，E:\plugins 实体与分发 junction 保留）。E free 26.4GB。
- 冒烟后 vox_setup.log 只有一行 start 是正常现象（junction 就绪时秒退）。

## 主包独立运行实测（2026-09-14，用 E:\plugins 改名隔离法验证）
- 方法：Rename-Item E:\plugins → plugins_hidden_for_test，包外 junction 全悬空，完美模拟"主包拷到无插件总仓的新机器"。
- 结果全部通过：首启自动完成（venv+离线解压 ~2.5min）；HTTP 8900 /api/status OK；/api/chat 提交 {"ok":true}（无 Key 时 SSE 返回供应商报错不崩溃——之前日志见过 "Illegal header value b'Bearer '"/无可用供应商降级，均为优雅报错）；/api/plugins 返回 0（插件页空，pkg_manager 扫描悬空 junction 无包，符合设计）；vox_setup 因 .setup_vox_failed 24h 退避秒跳过（之前 drive bug 时代写过失败标记——24h 后或删标记即重试）。
- 主包自足清单：app.py/server.py/settings_store/bridge/webui/FeiyuApp.exe/base python/offline_deps_core.zip。
- 测试后恢复：E:\plugins 改回、venv/标记/日志全清（未启动状态），junction 保留。
- 结论：**主包完全独立可跑**；E:\plugins 只增强（插件功能），不存在被反向依赖。vox fail 退避标记注意：E:\plugins 恢复后首启 vox 会被 24h 退避跳过（若存在 .setup_vox_failed），删标记即可立即接线（其实 junction 由 ensure_packs 建，vox_setup 只是兜底）。

## 插件市场上线（2026-09-14，最终架构：主体软件 + 空插件库 + UI 内安装）
- 用户最终诉求：standalone 作为软件主体，插件指向文件夹内的空插件库，需要时在 UI 内安装。
- 结构定型：主包 `plugins/`=真实目录（空插件库，预置 groups.json 库配置）；`E:\plugins`=仓库{plugins\16包, mc_pack, tools_pack, voice_pack, vl_pack}。
- 新增 `bridge/plugins_api.py` market_list/market_action：清单（代码包按 manifest、资源包按 PACK_TARGETS{voice_pack:[models,venv_vox], mc_pack:[mc_bot,mc_mod,_mc_ref], tools_pack:[tools], vl_pack:[hf_cache]}）+ 安装/卸载。代码包=copytree（首个插件自动带入 groups.json）+rescan；资源包=mklink junction（不复制 GB 级）；卸载代码包先 unload 再 rmtree；卸载资源包仅 os.rmdir junction 且**拒绝删真实目录**（安全护栏）。_market_repo 用 splitdrive+"\\\\"（drive bug 教训已固化）。
- server.py：GET/POST /api/plugins/market。
- webui：插件页顶部"插件市场"面板（表格+安装/卸载按钮+刷新市场），loadPlugins 联动 loadMarket；空库/仓库缺失均有友好提示。
- launcher：ensure_packs 弱化为仅兼容包内 libs/<pack> 旧布局；**移除 launch_vox_setup 自动调用**（venv_vox 由市场接线，防再出现误 pip 白装）；launch_vox_setup/_vox_setup.py 文件保留未删。
- 单测全过：清单 20 项（16+4）全未安装 → 装 greeting_demo（扫到 1 项）→ 卸载（库空）→ 装 voice_pack（models/venv_vox junction 经由访问实体 OK）→ 卸载（junction 消失实体无损）。
- 端到端冒烟：空库首启 → /api/plugins=0 + market=20 → HTTP 装/卸 greeting_demo 闭环 ✓。exe 重打包（launcher 改动；bridge/server/webui 是运行时磁盘文件不需重打包）。
- 当前状态：空插件库（groups.json 预置）+ 未启动 + 仓库完好。README 已更新市场语义。

## 全盘寻找插件功能（2026-09-14，seek 上线）
- 需求：UI 点「寻找插件」→ 全盘扫描能接入的插件 → 自动安装到 APP。
- `bridge/plugins_api.py` 加 `market_seek()`：ctypes GetLogicalDrives+GetDriveTypeW 只扫固定盘；os.walk topdown 限深 3 层；黑名单剪枝（windows/program files/appdata/node_modules/site-packages/venv*/hf_cache/qq_bot_runtime 等）+ 隐藏/$开头 + junction 不深入；120s 硬超时返回部分结果；跳过自身 APP_DIR 与 rt。
- 识别与自动安装：①代码包=目录含 manifest.json 且过收紧校验（name 蛇形正则 `^[a-z0-9][a-z0-9_-]*$` + kind∈{platform,feature,brain,sidecar,local}）→ copytree 入库+首插件带 groups.json；②资源包签名（voice=models/VoxCPM2+venv_vox/Scripts 双特征、tools=tools/ffmpeg、vl=hf_cache/hub、mc=mc_bot/package.json 或 mc_mod 含 jar）→ 部件级 junction 接线（已接线静默）。命中即剪枝不深入大件。装后 rescan。
- server.py POST /api/plugins/seek；webui 市场面板「寻找插件」按钮+结果框（loading/分类结果/刷新）。
- **测试教训（首轮 ALL FAIL 抓出的真问题）**：①判定过宽把浏览器组件（MEI Preload/WidevineCdm）和任意 manifest.json 目录误装——收紧为 feiyu 特征校验后排除；②用户盘有多个 feiyu 历史副本（F:\feiyu_standalone 完整旧包、E:\feiyu_rescue、F:\qq_bot、F:\qq_bot_release）——黑名单加 qq_bot_runtime 防深入 6 万文件树，found 去重防冗长；③首轮两个断言写错（接线优先级：E:\plugins 仓库版先于 seek_test 场景占用，幂等正确）。
- 复测 13/13 ALL PASS（真插件发现+自动安装、伪 manifest 拒绝、黑名单剪枝、voice/tools 接线可达实体、二次扫描幂等、卸载还原）。
- 收尾：E:\seek_test 与测试脚本已删、插件库恢复空（仅 groups.json）、测试 junction 已断（分发 junction hf/mc×3 保留）。
- bridge/server/webui 改动无需重打包（运行时读磁盘），仅 _launcher.py 变更才需要。

## 大脑随插件分发（2026-09-14，主包只剩 chat 大脑）
- 用户需求：除 chat 外的大脑全移出包体，与对应插件放一起，扫描插件时一同注册。
- 迁移：MC 大组 17 文件（mc_agent/common/watcher/skills/nav/survival/mods/recipes/recipe_usage/routines/tips/explored/critic/dashboard/monitor/game_watch/analyze）→ brain_mc_mod/engine/；mc_live.py → brain_mc_bot/engine/；pc_agent/control/intent → brain_pc/engine/；pvz_agent/vision/strategy/tips+tips.json → brain_pvz/engine/。
- **例外回迁**：mc_bot_brain.py/mc_bot_run.py 硬依赖引擎 config/identity/ai_provider 且是独立进程入口 → 留引擎（brain_mc_bot 插件=状态通道：mc_live+内联类）。教训：迁模块前先查引擎依赖 import。
- 4 个包装器重写：engine/ 注入 sys.path（brain_pvz 还注入 brain_pc/engine 因 pvz_agent→pc_control；manifest requires brain_pc 1.1.0）+ 内联大脑类（从 agent_core 搬）+ create_brain。brain_base（AgentBrain 基类）从引擎 sys.path import ✓。
- agent_core.py 手术：删 McModBrain/McBotBrain/PcBrain/PvzBrain 类（170 行）+ register_builtin_brains 只注册 ChatBrain；plugin_registry.py 内建清单删 4 条 brain spec。
- 容错化（主包底线）：chat_service 顶层 mc_watcher/mc_agent try/except→None；_McAsync.__getattr__ None 守卫（抛"MC 插件未安装"）；1496 MC 状态注入条件加 mc_watcher is not None 短路；intent_router 同容错+_game_online None→False（游戏意图总闸门短路）+execute_intent 防御守卫；self_knowledge pvz_tips 已在 try 内天然容错。
- **测试踩坑**：toggle 请求字段是 `on` 不是 `enabled`（server body.get("on")，bool(None)=False 走禁用方向 applied:true 假成功）——API 测试要对照路由实际字段名。
- 端到端验证：空库首启 brains=[chat] running=True + registry brain:1 + chat 提交 ok ✓；市场装 brain_mc_mod+toggle on → brains 出现 mc_mod [autonomous] ✓（engine 链路通）；卸载→回到 [chat] + chat ok ✓。
- 收尾未完成：venv 删除权限审批超时未执行 → 用户说"帮我删除"后已删 venv + core 标记，恢复标准未启动状态（E free 26.4GB）。

## GitHub 上传完成（2026-09-14）
- 仓库：https://github.com/hth768/Fat-Fish（Private），身份 hth768 <2190720017@qq.com>。
- .gitignore 关键排除：venv*/hf_cache/chat_history/models/mc_bot/mc_mod/tools/emojis/offline_deps_*.zip/pvz_games/data//_build/dist/__pycache__/*.log/venv_old_bak。
- 推送史：init → add（1521 文件）→ 发现 core zip 漏网（PowerShell 分号命令执行实际未生效的坑）→ gitignore 补规则重加 → 发现 venv_old_bak 漏网 → 补规则 + 清理提交 → 身份用 -c 参数临时传（用户名 hth768/邮箱 2190720017@qq.com，未写 config）→ push 被拒（远程有 Initial commit 占位 README）→ pull --rebase unrelated-histories 挂起（todo 混乱）→ **abort 后改用 git reset --soft origin/main + 单提交 847dd45（1511 文件/48.8 万行）→ push 成功 28a4617..847dd45**。
- 遗留瑕疵在库中：`nul; pyc` 垃圾文件（早前 cmd 重定向失误产物）、`.zcode/` 会话计划、`.setup_vox_failed`、`音色试听/*.wav`——可后续清理提交。
- git 经验：远程仅占位 README 时用 reset --soft origin/main 压单提交最干净（避免 force push 与 rebase 交互坑）；PowerShell 下 git stderr 进度输出会显示为 NativeCommandError（非真错误，看 exitCode 和 ref 更新行）。
- core zip 5.46GB 与资源包走 GitHub Releases 分发（拆卷）或网盘直链，未做。

## 发布构建 + 部署文档（2026-09-14，任务 1 完成、任务 2 已推送被网络卡住）
- `_split_release.py`：build（目录→zip DEFLATED level1→>1.9GB 自动分卷→**删 zip 中间件省一半空间**→SHA256SUMS.txt）/merge（分卷合并+哈希校验）/clean。TARGETS 顺序小件优先。
- **E 盘两次爆盘**（Errno 28）：zip 原件+分卷双份存储是主因；E 盘回收站两次吃 12.4GB（Remove-Item 安全删除又进回收站）——rd SID 目录恢复 18.1GB。教训：大文件构建必须"拆卷即删中间件"，删除一律用 cmd del，构建前查回收站。
- dist 终态（13 文件）：feiyu_core.part1/2/3（5.5GB）+ voice_pack_7z.part1/2/3（4.4GB）+ vl_pack_7z.part1-4（5.9GB）+ mc_pack.zip 0.23 + tools_pack.zip 0.29 + plugins_pack.zip 0.2 + SHA256SUMS.txt。命名不统一（feiyu_core 无 _7z、其他有）——历史原因，merge 用各自 prefix 即可。
- 断点续跑判定：分卷齐或单 zip 在即 skip（含 core）。
- README 重写：仓库结构（主包~1.5GB + <盘>:\plugins 仓库）/快速开始/分卷合并命令/**插件一览三表（平台/功能/大脑，各插件独立行：功能+所需资源包+额外依赖）**/资源包表/寻找插件说明/维护者发布命令。
- git：docs 提交成功（3 文件），push 三连败（github.com:443 Connection reset/超时——网络波动，首次 757MB 大推送时是通的）。**本地领先 1 提交待推**，网络恢复后 `git push` 即可。
- 用户上传大文件到 Release：gh CLI 或网页拖拽 dist/ 全部文件（单资产 <2GiB 已保证）。
- docs 提交已推送成功（847dd45..7a8fd0e，网络恢复）。仓库两提交齐：init README + 全部项目 + docs。

## Release 全部发布完成（2026-09-15）
- 用户分批网页上传完成 13 资产：core 三卷（part2 中途失败重传成功）、4 小件、voice 三卷、vl 四卷 → 已发布。
- README 资源包表加"已发布 ✓"+ Releases 链接 + 合并命令，提交 042b12f 推送成功。
- 分发链路全通：clone → Release 下 core 三卷合并放 libs/ → 双击 exe → 插件市场按需装。
- 网页大文件上传经验：一次拖全部必炸，分批+失败重试+每批保存草稿；失败项点 × 删了重传即可，互不影响。

## napcat_pack 加入 Release（2026-09-15）
- E:\NapCat（339MB 源，zip 130MB）= NapCat QQ NT 框架（bootmain/napcat/win64/napcat.bat 等）。
- 加入 _split_release.py TARGETS（napcat_pack，根目录名 napcat）；不进 PACK_TARGETS 接线体系——NapCat 独立运行（napcat.bat 启动，反向 WS 指向 qq_platform 的 ws://127.0.0.1:8080）。
- 顺手修复：skip 分支文件型产物（core）哈希丢 N/A → sha256(src)（分卷合并目标与源同内容）。验证全部 7 项真哈希。
- README：qq_platform 行加 napcat_pack 依赖 + QQ 平台完整安装流程引用块 + 资源包表加行。commit d788a5f 已推送。
- 用户侧待做：Release 编辑页上传 dist\napcat_pack.zip（130MB 小文件很稳）。
