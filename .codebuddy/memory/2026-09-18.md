# 2026-09-18 工作日志

## 修复：选完主题色再调回外观页会跳回深夜蓝
- 现象：用户在外观页选了某个主题色，没点「保存外观」就切到别的页面，再进外观页时主题又变回「暗夜蓝（midnight）」。
- 根因：`webui/app.js` 的侧栏导航 `switchSection`（行 45-47）里 `appearance: loadAppearance`——**每次进入外观页都会调用 `loadAppearance()`**，它重新 `GET /api/appearance` 并 `applyVars` 覆盖当前主题。而选主题色的点击处理只把 `appearance.theme` 写入内存变量并 `previewTheme()` 临时预览，**并未持久化**；只有点「保存外观」才会 POST 到后端。于是未保存就切走、再进外观页即从后端拉回旧主题（默认 midnight）→ 跳回。
- 修复：`webui/app.js` 新增 `saveTheme(name)`（`POST /api/appearance {theme}`），并在 `renderThemeGrid` 的主题色点击回调里于 `previewTheme` 之后调用 `saveTheme(appearance.theme)`——**选主题即时持久化**。后端 `save(theme=...)` 只更新 theme、不动 title，安全。这样无论是否点「保存外观」，切走再回来都是新主题。
- 验证：重启 E:/feiyu_app 实例（旧 24256 被杀，新 PID 19336 监听 8900）后，`POST {theme:forest}` 保存，再 `GET /api/appearance`（模拟进外观页 loadAppearance）返回 `theme=forest`、`--bg=#0c1611`（森林绿调，非 midnight），确认不再跳回；HTTP 返回的 `app.js` 含 `function saveTheme`。测试后已把持久化主题恢复为默认 midnight。
- 注意：之前多次重启发现「监听 8900 的 PID 与新启动实例不符」——因 server 改为端口独占绑定，若旧实例仍在（且 cmdline 匹配未命中 `feiyu_app\app.py` 的筛选），新实例会启动即退出；需用 `netstat` 找到 8900 监听 PID 精准杀掉再起。
- 文件：`webui/app.js`（前端，无后端改动，后端 save/get_appearance 本就正确用 theme）。

## 构建助手升级为「能读文件 / 有历史 / 能改进」的 Agent
- 需求：用户要求把「构建助手」做成能读取文件、有历史文件、能改进的 Agent，并推送。
- 现状：构建助手 = `bridge/builder_api.py` 经 LLM 一句话生成 agent.json / 插件包并落盘（`server.py` 路由 `/api/builder/*`）。
- 改动：
  - `bridge/builder_api.py`：① 文件上下文 `list_context_file`/`read_context_file`（`_is_allowed_context_path` 用前缀匹配 `libs/qq_bot_runtime`/`bridge`/`webui`/`plugins` 白名单 + 大小上限，路径守卫坑：最初用顶级目录 `libs` 匹配 `libs/qq_bot_runtime` 失败，已改为前缀匹配）；② 构建历史持久化 `data/builder_history.jsonl`（jsonl 追加，`append_history`/`get_history`/`_trim_history`，每条记录 event/mode/ok/requirement/key/artifact）；③ 新增 `improve_agent`/`improve_plugin`，读取磁盘已有产物按指令改进（校验同生成流程）；④ 生成时经 `_history_block` 注入最近 2 条同类型成功案例作为 few-shot 改进范例（use_history 默认开）。
  - `server.py`：新增 `/api/builder/files`、`/api/builder/file/read`、`/api/builder/history`，并给 generate/improve 接口加 `context_paths` + `use_history`。
  - `webui/index.html`+`app.js`+`styles.css`：构建页新增「参考文件」面板（勾选源码作上下文）与「构建历史」面板（每条可「载入」/「改进」；「改进」会进入改进模式，输入框预填「改进：<原需求>」、生成键变「改进生成」）。`builder` 对象新增 `contextPaths`/`improve` 状态。
- 验证：`py_compile` 通过；纯函数测试 `list_context_files`(149 文件)、`_context_block`、路径守卫 True/False 正确；mock LLM 跑通 `improve_agent`（落盘 1 条 history，`improved_from=feiyu`），`improve_plugin` 对不存在插件正确报错。
- 提交 `d6b30f4` 并推送 `35e4f91..d6b30f4 main -> main`。
- 注意：本次临时测试脚本 `_tmp_build_test.py` 因删除需批准且超时未删，仍留工作区（未跟踪，不会被提交）；`data/builder_history.jsonl` 被 `/data/` 忽略。

## 构建助手：增加工作区读写（类代码 Agent，可反复改/debug/升级）
- 需求：用户在构建助手里改类文件、能像 codebuddy 等代码 Agent 一样对插件代码反复修改/debug/升级。
- 实现（提交 `b4e3bcf`，+421 行）：
  - `bridge/builder_api.py` 新增 5 个 `*_sync` 函数：`list_workspace_sync(dir)`、`read_workspace_sync(path)`、`write_workspace_sync(path, content, backup=True)`、`workspace_diff_sync(path, content)`、`list_plugin_files_sync(name)`。
  - `server.py` 新增 5 个路由：`/api/builder/workspace/{list,read,write,diff}`、`/api/builder/plugin/files`。
  - 安全：`_WORKSPACE_ROOTS = (plugins, libs, bridge, webui, agents, config)` 白名单；`_WORKSPACE_DENY` 强制拒绝 `.codebuddy/`、`.env`、`ai_providers.json`、`settings_store.py` 等；二次 `realpath` 校验防越界；`_is_text_file` 拒二进制；单文件 512KB 上限。
  - 自动备份：每次写之前 `shutil.copy2` 到 `data/builder_bak/<rel>.<YYYYMMDD_HHMMSS_microsec>`，每文件最多 20 份，超出按 mtime 删。
  - 前端：构建助手页末新增「工作区源码读写」面板：左栏 = 路径输入 + 快捷根（plugins/bridge/webui/libs）+ 文件/目录双击进入；右栏 = 路径 + textarea + 读/写/查看 diff；底部 details = 按插件名载入包内所有文件（一键跳到该文件）。
- 验证：
  - 白名单路径可读写可 diff（如 `plugins/bilibili_learn/manifest.json` 实际读出 453B）。
  - 黑名单全部拒绝（`.codebuddy/...`、`E:/qq_bot/ai_providers.json`、`qq_bot/ai_providers.json`）。
  - 自动备份实测：首次写 `bridge/builder_api.py`（测试脚本误操作！）备份到 `data/builder_bak/bridge/builder_api.py.20260918_174326_479530`，然后从备份完整还原 1024 行；之后用 `plugins/groups.json`（原不存在）写测试，`backup=null`，写入后 `Remove-Item` 还原。
  - 实测对 bilibili_learn 真实插件包 `list_plugin_files_sync` 返回 `manifest.json`(453B) + `plugin.py`(334B)，`read_workspace_sync` 完整读出 manifest 内容；接口耗时 < 100ms。
- 同步到运行时实例 `E:/feiyu_app` 4 个文件 + 重启（PID 15892）；E:/feiyu_app 实际 APP_DIR = feiyu_app 自身，运行时插件在 `E:/feiyu_app/plugins/`（由 qq_bot 引擎同步注入），与开发副本 `feiyu_standalone/plugins/`（空）不对称——UI 默认进入目录从 `libs` 改为 `bridge`（运行时 feiyu_app 没 libs，但有 bridge/webui/plugins）。
- 顺手清理：删除 `libs/qq_bot_runtime/{agents/agent_20260917010926, agents/ext_x, data, important_notes.json, knowledge_base.json, user_profiles.json}` 9-17 智能体导入测试残留运行态污染（`git status -s` 现只剩本次 5 个改动）。

## 推送：构建助手两轮升级 + 记忆文档
- 用户要求「推送构建助手改动以及之前未提交的改动」。核查后：工作区完全干净（`--untracked-files=all` 无输出），无任何未提交改动；此前会话残留的运行数据文件已在上条清理中删除，从未进入版本控制。
- 当时本地领先远端 3 个提交，全部推送（`d6b30f4..e088e30 main -> main`）：`b4e3bcf` 构建助手工作区读写能力、`982c37c`+`e088e30` 记忆文档（MEMORY.md / 2026-09-18.md）。`d6b30f4`（上下文/历史/改进）上一轮已推送。
- 推送前安全核查：`git diff origin/main..HEAD` 扫描 `sk-*`/`AIza*`/`ghp_*`/`Bearer` 等密钥特征，无命中；MEMORY.md 新增段仅描述能力与路径，无密钥。
- 备注：`.codebuddy/memory/*.md` 自 09-14 起即被仓库跟踪，记忆文档提交属既有惯例。

## 同步构建助手改动到部署版 E:/feiyu_app + 顺手修复 GET 路由 bug
- 用户：「把改动同步到独立版」。核查结论：`e:\qq_bot` 是**纯引擎副本**（无 `bridge/`/`server.py`/`app.py`/`plugins/`，其 `webui/` 是引擎自带控制台，09-08 的老文件、无构建助手代码），**无法承载 App 层改动**；唯一含 App 层且正在运行的是 `E:/feiyu_app`（进程 `E:\qq_bot\venv\...\python.exe E:\feiyu_app\app.py --with-core`）。故同步目标取 E:/feiyu_app。
- 比对：`bridge/builder_api.py`、`server.py`、`webui/index.html` 已一致（上一轮 17:40 同步）；落后的 6 个文件已备份到 `E:\feiyu_app\data\_sync_bak_20260918_182326\` 后同步：
  - `webui/app.js`（含默认工作区目录 `libs`→`bridge` 修复——部署版无 libs 目录，旧的 libs 版会点空）
  - `webui/styles.css`（构建助手 参考文件/历史 样式块）
  - `bridge/plugins_api.py`（3.0KB/09-12 → 24.3KB）、`bridge/pkg_manager.py`（12.9KB → 14.4KB）
  - `plugins/groups.json`、`README.md`
  - **必要性**：已同步的新 `server.py` 会调用 `group_action`/`market_list`/`market_seek`/`market_action`/`plugin_config_view`/`plugin_config_save`，而部署版旧 `plugins_api.py` 完全没有这些函数 → 插件页分组/市场/配置功能此前实为坏的。
- **发现并修复真实 bug**：`/api/builder/{files,file/read,history}` 三个只读接口被误注册在 `do_POST` 分支，而前端用 `GET()` 调用 → 「参考文件」「构建历史」面板 404。已移到 `do_GET`（`?path=` / `?limit=` 传参），`do_POST` 只留写操作。提交 `2573bb7`，推送 `471f01e..2573bb7`。
- 重启两次实例（第一次载入新 bridge 模块，第二次载入修复后 server.py）：最终 PID 8732 监听 8900。
- 实测通过：`GET /api/builder/files` → `ok:true`（149 文件）、`GET /api/builder/history` → `ok:true`、`GET /api/plugins` 10KB 含 console_platform（证明新 plugins_api 工作）、`GET /api/status` core:true、静态资源含 `_bldRefreshHistory` / `_wsLoadDir("bridge")` / `ctx-files`。
