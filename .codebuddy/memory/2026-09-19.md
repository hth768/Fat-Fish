# 2026-09-19

## 防串台对照（昨日截图启发）
- 用户贴另一会话截图：Cortico vs qq_bot 插件对比，判定"E 盘 qq_bot 无名单无会话概念"。
- 本会话做 8 维只读对照（code-explorer 子代理）：引擎其实有 user_id 记忆隔离 + @门控 + 出站同源派生 + [称呼]全量前缀 + get_msg 引用还原 + post_type 回声排除；**实质缺口=群聊记忆混用**（memory.py `_key` 仅 `user:{id}`，跨群共享上下文）、无群白名单、无 message_id LRU、无群级 envPrompt。对比结论部分低估引擎（它比的是宿主侧封装而非引擎本身）。

## QQ 群监听白名单（用户选"加群白名单，不动记忆"）
- 实现（未破坏记忆键，保持现有跨群共享）：
  - `config.py`：`QQ_GROUP_WHITELIST = []`（空=关闭=所有群处理；非空=仅处理列表内群号字符串）。注释对齐 B 站 `BILIBILI_DM_WHITELIST` 安全模型。
  - `qq_plugin.py:621` 群消息分发入口（`_handler` 群分支）：`gid` 不在白名单则 `continue` —— 真 LISTEN 过滤，非白名单群不组装/不分发/不调 LLM/不回复。
  - `qq_plugin.py:start()`：启动时打印白名单启用状态（仿 B 站 `[BILI_DM] 白名单 UID` 提示）。
- 私聊不受影响（仍走 ONLY_MENTION_OR_PRIVATE）。
- `py_compile` 通过，`read_lints` 零错误。**尚未提交/部署**（用户未要求；默认空列表不改动线上行为）。
- 待定：是否提交并同步 `e:\qq_bot` + 重启 `E:\feiyu_app`（配置默认空，部署无行为变化，但换副本仍是好实践）。

## 提交 + 部署（用户"都做"）
- 提交 `c9404de`（2 文件，+17），已 push `f19aa04..c9404de main -> main`。
- 同步 `config.py`/`qq_plugin.py` → `e:\qq_bot\`（哈希一致）；强杀旧 45912/40924/30188，detached 重启（`_boot_qqwl.log`）。
- 新进程 53896/53592/50508 健康；日志确认「群监听白名单未启用（QQ_GROUP_WHITELIST 为空）」；8900 LISTENING。
- 注：线上仍默认空白名单=所有群处理（预期，配置生效开关）。要启用须手动在 `e:\qq_bot\config.py` 填群号。
- 工作区残留未提交：`_dbg_mem_tmp.py`（调试临时）、`.codebuddy/memory/2026-09-18.md`（前日日志，今日另有 2026-09-19.md）。

## 多 bot 隔离架构（进行中任务）——实时检测
- 任务：运行时多 bot。`BotManager`(`bridge/bot_manager.py` 新增) 按 agent_id 增删/启停，`get_core` 单例缓存(`_core_instances`)；注册表 `data/bots.json`；默认主 bot=`feiyu`。
- 隔离机制：`agent_ctx.agent_storage_dir()`（feiyu 回落引擎目录不迁移，其余落 `agents/<id>/memory`）；`chat_service.handle_message` 边界 `set_agent(self.agent_id)` + contextvars 驱动；`vector_memory`/`memory.py` 按 `agent:user_id` 分桶；persona 经 `core._persona_override` 注入。
- **检测到的严重 bug（已修）**：默认 feiyu 主 bot 被 `_async_start()`（`get_core()`→feiyu）与 `autostart_all()`（`get_core("feiyu")` 同一实例）重复 `start()`——二次 core.start + 插件/ build_hook 重复注册。修复：`_async_start` 启动后 `self.bot_manager.cores[core.agent_id]=core`；`_async_stop` 同步 `bot_manager.cores.pop(agent_id)` 防 is_running/start 误判。编译+lint 通过。
- 其余改动（long_term/persona/reflection/important_notes `_ns_base()`、chat_service persona 覆盖、core_bridge `submit_chat(bot_id=)` 路由、_dbg_mem_tmp quiet.degrade）编译+lint 全通过，无新错误。
- 次要：`_dbg_mem_tmp.py` 仍硬编码 `f:\qq_bot`（本机无 F: 盘），调试残留，不影响功能。

## 多 bot 前端切换 + 部署验证（完成）
- 前端（`webui/app.js` + `webui/index.html` + `webui/styles.css`）：仪表盘/记忆/总结/配置四页头部加 `#botSwitcher` 下拉 + "管理 Bot" 按钮；新增 `botModal`（列表 + 新建/编辑/启动/停止/删除）。`state.botId` 默认 `feiyu`，新增 `GB()`（带 `bot_id` 的 GET）/`PB()`（带 `bot_id` 的 POST）包装，记忆/总结/配置/导入/动作类请求全部走 `bot_id`；配置页新增「当前 Bot 专属配置」面板（人格/模型/插件/自启，经 `update_bot` 保存）。仪表盘 `renderBotCard` 显示所选 bot。
- 后端一致性修复：`server.py`/`summary_api.py` 调用的是 `agent_ctx.reset_agent(token)`，原误写为 `agent_ctx.reset`（`AttributeError`）。已改并重新同步 `E:\feiyu_app` + 重启验证。
- `delete_bot` 仅删 `memory` 子目录留空 `agents/<id>` 父目录——已改为删除整个 `agents/<id>` 命名空间。
- 部署：同步 `libs/qq_bot_runtime/*`（8 文件）→`e:\qq_bot`、`server.py`/`bridge/*`(含新 `bot_manager.py`)/`webui/*`→`E:\feiyu_app`，哈希一致；强杀重启，8900 LISTENING。
- 功能实测：`/api/bots` 返回默认 feiyu；创建 `test_bot_a`→启动→列表出现→`agents/test_bot_a/memory` 懒创建；`/api/memory/stats?bot_id=test_bot_a` 显示 profiles/notes/persona/reflection=0（隔离生效）；停止→删除后列表仅剩 feiyu，目录清理干净。
- 已知小缺口：`knowledge`（vector_memory）未完全按 bot 隔离，仍读全局知识库；人格/笔记/用户档案/反思已隔离。模型覆盖 `_model_override` 已存储但未在 chat_service 实际消费（占位）。
- 待办：git 提交 + push（多 bot 全部改动）。

## 补全多 Bot 两个缺口（完成）
- 缺口1 知识库隔离：`knowledge_store._kb_path()` 改为经 `agent_ctx.agent_storage_dir()` 选路径（feiyu 回落引擎目录，其余 `agents/<id>/memory/knowledge_base.json`）；`_save` 增加 `os.makedirs` 保证目录存在；`import agent_ctx`。记忆页统计/导入导出/学习沉淀(`add_entry`)各 bot 互不串台。引擎级实测：botB 写 `agents/test_bot_b/memory/knowledge_base.json`(count=1)，feiyu 仍读引擎目录(40 条不变)。
- 缺口2 模型覆盖消费：`chat_service._chat_pipeline` 主回复 `model=` 改为 `config.DEEPSEEK_REASONER_MODEL if use_reasoner else (getattr(self.core,'_model_override',None) or None)`，推理模型不受影响。bot_manager `_build_core` 已设 `core._model_override=spec.get('model') or None`。
- 注：HTTP 测试中文经 PowerShell `-Body` 会被 GBK 破坏导致 JSON 解析为空（kind=""→「不支持的操作」），改用 ASCII 或走 UI 即可；非代码缺陷。
- 提交 `3960c16` 并 push；已同步 `e:\qq_bot`（knowledge_store/chat_service/agent_ctx）+ 重启 `E:\feiyu_app` 验证。

## README 更新 + 推送（完成）
- README 新增「多 Bot（多智能体切换）」章节（目录/agent_ctx 表/专节），说明动态管理、完全隔离、模型覆盖、后端路由；配置页专属配置面板提示。
- 提交用 `git commit -m`（非 UTF-8 `-F`），导致提交信息被 GBK 破坏乱码（`5c676f3`）。**教训：中文提交信息必须走 UTF-8 文件 + `git commit -F`，勿用 `-m`**——即便刚同步过该约定仍踩坑。该提交已 push，按安全规则不强制推送重写；如需修正消息需 force push（用户许可后再做）。
- 用户确认「修正」→ `git commit --amend -F` 重写字utf-8消息 + `git push --force-with-lease` 成功（`5c676f3...10969bb` main -> main，forced update）。中文提交信息约定（UTF-8 文件 + -F，禁 -m）已再次验证有效。

## 同步方向纠正 + 仓库→部署刷新（完成）
- 用户「把所有改动都同步到feiyu_standalone」初判为部署→仓库，但经核实**方向反了**：仓库才是更新的真相源。证据：① 仓库 `self_coding.py`/构建助手 + Issue 机制（`/同意issue`）已取代部署陈旧的 `codebuddy_cli.py`（外部 CLI 旧方案，`config.CODEBUDDY_*`）；② 仓库 `plugin_registry` 仅内置 `chat` 大脑，游戏大脑改由 `brain_*` 插件包自带（部署仍把 mc_bot/mc_mod/pc/pvz 写进内建清单=陈旧）。drift 报告各文件 repo-only 行数均 > deploy-only。
- 用户确认后改为**仓库→本地部署副本**刷新（推荐方案）。
- 漂移扫描 `_drift_scan.py`：比对 `libs/qq_bot_runtime/*.py` vs `e:\qq_bot\*.py` + bridge/server/webui。发现部署副本 27 个独有 .py（游戏 `mc_*`/`pc_*`/`pvz_*` + `codebuddy_cli`）不在仓库——同步时**保留不删**。仓库需新增到部署：`agent_runtime.py`、`self_coding.py`。
- 执行同步脚本（复制/新增、不删）：引擎 `libs/qq_bot_runtime/*.py`→`e:\qq_bot`（127 覆盖 + 2 新增），App `bridge/* server.py app.py settings_store.py webui/* tests/* docs plugins/groups.json + greeting_demo/mc_world 包`→`E:\feiyu_app`。关键文件先备份到 `_sync_backup/20260919_202935/`。
- 验证：`config.py` 部署已含 `BOT_SELF_CODING_ENABLED`、无 `CODEBUDDY`；`ai_providers.json`（真实 Key）保留；`mc_agent.py` 等游戏模块保留；重跑漂移扫描 = `NO DRIFT`。
- 重启 `E:\feiyu_app`（强杀 49360/52344/27632，detached `--with-core` 重拉，`multibot_deploy.log`）：App 就绪 8900，核心启动，大脑仅 `chat`，QQ/控制台/B站/sidecar 全部就绪，无导入/启动错误（日志中 `[REGISTRY] web` 提示与余额 WARN 为既有配置问题，无关）。

## Self Coding 接入配置页 + Issue 栏（完成）
- 需求：把 `BOT_SELF_CODING_*` 开关搬进配置 UI，并增加 Issue 管理栏。
- 后端：`settings_store.CONFIG_SCHEMA` 新增分区「智能体自编程（Self Coding）」4 项（ENABLED/ISSUE_AUTO/AUTO_LOAD 为 bool，PERM 为 `select` 下拉 plan/default/acceptEdits/full/bypassPermissions）；`config_api.config_view` 透传 `options` 给前端。新增端点 `GET /api/self_coding/issues`（list_issues+is_enabled）、`POST /api/self_coding/approve|reject|file`（直调 `self_coding` 模块，引擎层 import 延迟到路由内避免 import 顺序问题）。
- 前端：`webui/app.js` `cfgItem` 新增 `select` 渲染并监听 change 标脏；`index.html` 配置页加 `#scPanel` Issue 队列（状态条 + 列表 + 提交框）；`loadSelfCoding()` 渲染/同意/拒绝/提交，`loadConfig` 内调用。`styles.css` 加 `.sc-issue`/状态徽章样式。
- 验证：`node --check` 通过；`/api/config` 已下发 self-coding 分区（PERM value='full' + 5 options）；`/api/self_coding/issues` 返回 enabled=False（默认关）。已同步 App 层 6 文件到 `E:\feiyu_app`（备份 `_sync_backup/ui2_20260919_204525/`），重启 App 健康检查通过。

## 侧边栏新增「自编程 Issue」独立页（完成）
- 需求：把 Issue 历史从配置页内嵌面板提升为侧边栏独立入口。
- `index.html`：侧栏在「配置」与「构建助手」间加 `<a data-page="issues">自编程 Issue</a>`；配置页原 scPanel 内嵌面板改为挂载点 `#issueMountConfig`（简版仍显示）；新增独立页 `#page-issues`（含 `#issueMountPage` + Bot 切换器）。
- `app.js`：把 `loadSelfCoding()` 重构为通用 `mountIssues(container)`（自包含注入状态条/列表/提交框并绑定事件，避免旧全局 ID 冲突）；`loadConfig` 调用 `mountIssues(#issueMountConfig)`；新增 `loadIssuesPage()` 挂 `#issueMountPage`，并加入页面加载映射 `loaders.issues`。
- 验证：`node --check` 通过；重启 App 后 `/api/self_coding/issues` 正常（enabled=True，count=0，开关此前已被覆盖层置开）。已同步 `webui/app.js`+`index.html` 到 `E:\feiyu_app`（备份 `_sync_backup/ui3_20260919_205430/`）。
