# 2026-09-18

## C 盘清理（用户请求「帮我清理C盘」）
- 现状：C: 120.7 GB **剩余 0 字节**（Windows 30.6 GB / Users 34.9 GB / Program Files 16.3 GB，WinSxS 12.3 GB）。
- 本机磁盘：单块 NVMe 477 GB → C 121 + D 153 + E 173。**D 盘几乎空着（137.9 GB 空闲）**，E 盘只剩 9.9 GB。以后找迁移目标优先 D。
- 已完成（第一轮，零风险，释放 5.5 GB → C 剩 5.52 GB）：
  - 用户 `%TEMP%` 1.62 GB、CrashDumps 62 MB、`.workbuddy\{logs,traces,blobs}` 315 MB、`.codex\.tmp` 78 MB
  - `@genieworkbuddy-desktop-updater` 463 MB + `@zcodedesktop-updater` 156 MB、`pnpm-cache` 144 MB
  - Edge `Cache/Code Cache/GPUCache/Service Worker` 约 390 MB
  - `.chromium-browser-snapshots` 1.37 GB（puppeteer 缓存，改用系统 Edge 后无用了）、`.cache\codex-runtimes` 1.33 GB（可重下）
- 已完成（第二轮，5.52 → 8.18 GB）：Downloads 里 25 个已安装的安装包/压缩包/crdownload 共 1.75 GB（用户选择「直接删除」）；`AppData\Roaming\BCUT\Cache` 950 MB。
- 已完成（提权）：`powercfg -h off` 删掉 `hiberfil.sys` **+6.14 GB**（8.18 → 14.32 GB）。副作用：无休眠/快速启动。DISM `/StartComponentCleanup /ResetBase` 随后执行。
- 用户明确**暂缓**：把 C 盘页面文件（17.84 GB，init 8 GB/max 24 GB）搬到 D 盘——「先记着，之后再做」。这是最大单笔可回收空间，后续要做时需管理员 + 重启。
- 用户**没选**的（本次不动）：`.gradle` 缓存 2.3 GB、Minecraft 缓存 1.85 GB、OneDrive 缓存 0.83 GB。
- 未动但体积可观：`C:\Windows\System32\DriverStore` 6.64 GB、`AppData\Roaming\Tencent\xwechat` 1.27 GB + QQMusic 812 MB + WeMeet 493 MB、`AppData\Local\Qianwen` 1.51 GB / `OpenClawPlanTool` 979 MB / `OpenAI` 699 MB / `CodeBuddyExtension` 680 MB、`.minecraft` 2.04 GB、`baidunetdisk` 511 MB。
- 工具落点：临时脚本/日志都放在 `e:\feiyu_standalone\.codebuddy\`（`_scan_c*.ps1`、`_clean_c*.ps1`、`_elev_clean.ps1`、对应 `.txt`/`.log`），任务结束后清理。
- 踩坑复现：PowerShell 脚本里写中文 → 被 GBK 破坏导致 parser error（`\"{0,-40}\" -f` 里的中文全角字符吃掉引号）。**脚本一律纯 ASCII**，输出到文件再读。

## DISM 清理在这台机器上跑不通（重要环境结论）
- `Dism /Online /Cleanup-Image /StartComponentCleanup [/ResetBase]` **一律失败，错误 5 拒绝访问**（试了 3 次：带/不带 ResetBase、sfc 之后再试）。
- CBS 日志 `C:\Windows\Logs\CBS\CBS.log` 两个关键报错：
  1. `CorruptPayloadFile` + `STATUS_SXS_FILE_HASH_MISMATCH`（scavenge.cpp:1842）→ 组件库里有载荷文件哈希对不上（疑似被 360「系统盘瘦身」类功能动过）。
  2. `STATUS_CANNOT_DELETE` 卡在 `C:\Windows\WinSxS\Catalogs\5096931d….cat` → 文件被占用删不掉，最终 `Failed to commit scavenging of CSI store [E_ACCESSDENIED]`。
- 机器上装着 **360（`C:\Program Files (x86)\360\360Safe`），进程 `ZhuDongFangYu`（主动防御）常驻** —— 锁定 WinSxS 目录的典型元凶。
- `sfc /scannow` 返回 **exitcode 0（未发现完整性冲突）**，别指望 sfc 能解。
- 后续重试路径：先让用户**退出 360 主动防御**（或进安全模式）再跑 DISM；否则直接放弃这条路径（WinSxS 12.26 GB 动不了）。
- 附带结论：**本机 UAC 提权方式可用** —— `Start-Process powershell -Verb RunAs -ArgumentList '-NoProfile','-ExecutionPolicy','Bypass','-WindowStyle','Hidden','-File','<脚本>'` 会弹 UAC，用户点「是」后能正常执行提权操作（`powercfg -h off` 就是这么跑通的）。提权脚本内部**不要用 `-RedirectStandardOutput`**（与 ShellExecute 冲突），改成脚本内 `Out-File`。

## 修复控制台日志刷屏（提交 cddf082）
- 用户贴了启动日志：同一批 `[PKG][WARN] xxx manifest 提醒: 建议补 schema_version…` 重复十几屏，另有 16+ 条 `[BILI] 已获取匿名 buvid`、一条 `[pywebview] FOLDER_DIALOG is deprecated`。全是我前几轮自己引入的噪音。
- **三处修复**：
  1. `bridge/pkg_manager.py`：根因是 `scan_packages()` 被**高频调用**（状态轮询、插件页刷新、各处 `get_package`），而告警在每次扫描时逐包 `print`。改为：`_log_manifest_issues()` 按「包 + manifest mtime + 内容签名」去重（首次或 manifest 变更才打印）；`schema_version` 缺失由 `_flush_schema_notice()` **跨包聚合成一行**（包集合变化才重打）。`read_manifest()` 也走同一去重。
  2. `libs/qq_bot_runtime/bili_api.py`：buvid 提示原是**实例级** `self._fetched` 守卫，但进程内有多个 `BiliApi` 实例 → 每实例各打一次。改为模块级标志 `_BUVID_NOTICE_DONE` / `_BUVID_FAIL_NOTICE_DONE`，整进程各打一次（失败提示补「后续不再重复提示」）。**该文件属引擎层，必须同步 `e:\qq_bot\bili_api.py`**。
  3. `bridge/app_window.py`：`pick_folder()` 用了已废弃的 `webview.FOLDER_DIALOG` → 每次调用打 deprecation 警告。改为优先 `FileDialog.FOLDER`（venv 里已确认存在），旧版才回退 `FOLDER_DIALOG`，都没有则返回 `supported=False`。
- **验证**：
  - 本地单测：5 次 `scan_packages()` 零输出；新建缺 title 的包 → 只提示一次；改为非法 kind 后 3 次扫描仅 1 条错误 + 1 条提醒；4 次 `read_manifest()` 仅 1 行；bili 标志与源码分支正确。
  - 部署版带日志重启（stdout 落盘统计）：**启动日志 40 行**，`[PKG]` **2 行**（1 条聚合「15 个插件包未声明 schema_version…」+ 1 条预装包提示，原为十几屏）、`[BILI]` **2 行**（原 16+）、**deprecated 0 行**；`[SIDECAR]` 3 行带 log 路径 ✓。
  - 功能无回归：`/api/plugins` 16 个包、15 个仍带 `manifest_warnings` 字段、无 `invalid` 包、`/api/status` core 运行中。
- **新增/固化的姿势**：启动实例时把 stdout 重定向到文件以便统计噪音 ——
  `Start-Process cmd.exe -ArgumentList '/c', "set FEIYU_QQ_BOT=E:\qq_bot&& ... app.py --with-core > <日志> 2>&1"`（写在 `.ps1` 里）；**Python `subprocess.Popen(["cmd.exe","/c",...])` 那种方式在本机起不来**（实例没起、日志没生成），浪费了一轮。统计脚本要 `sys.stdout.reconfigure(encoding="utf-8", errors="replace")`，否则日志里的替换字符会触发 GBK `UnicodeEncodeError`。
- 提交 `cddf082`（3 文件，+75/-10），推送 `0a07021..cddf082`；已同步部署版（`bridge/pkg_manager.py`、`bridge/app_window.py`）与引擎副本（`bili_api.py`，备份在 `E:\feiyu_app\_backup_20260918_logfix\`、`e:\qq_bot\_backup_20260918_logfix\`）并重启（PID 45900）。
- **注意**：本轮发现另一并发会话把 `2026-09-18.md` 与 `MEMORY.md` **整体改写/压缩**过（未提交），我保留了对方内容并追加本条，同时把 MEMORY.md 里「pick_folder 用 FOLDER_DIALOG」的过时描述改为 `FileDialog.FOLDER`，并新增第 8 节「日志噪音约定」。.codebuddy 下另有对方留下的 `_scan_c*.ps1` / `_clean_c*.ps1` 等未跟踪脚本（C 盘清理任务用），未动。

## 回应「构建助手对项目的批判性评估」（提交 ab383da）
- 用户发了 3 张构建助手自我评估的截图（`20_20_34` / `20_20_51` / `20_21_04`）：它读了 GitHub 仓库后给出「隐忧/风险」四条（bus factor 极低 / 广度换来「重」与可维护性（捆绑运行时、9.7GB voice_pack、裸 except 遍地、无测试）/ 安全合规面缺失 / 抽象可能过度）+ 星级打分 + 三条后续建议（当范例拆解、揪工程问题、补 LICENSE·收紧异常·写最小测试）。
- **先核实再动手（不照单全收）**，实测结论：
  - ✓ **LICENSE 确缺**（`LICENSE*`/`COPYING` 全无，README 也没提 license）。
  - ✓ **无 App 层测试**；但**「无测试」不完全准确** —— 引擎层早有 `libs/qq_bot_runtime/tests/`（90 项，`run.py` 一键跑）。App 层 `bridge/` 确实零测试。
  - △ **「裸 except 遍地」需精确定性**：真正的裸 `except:`（不带类型）= **0**；`except Exception:` 共 **692 处**（bridge 159，engine 500）。真正的问题是**静默吞掉**（`except …: pass/continue`）**259 处**（bridge/builder_api.py 34、mc_bot_brain.py 23、memory_api.py 17…）—— 这才是"异常静默降级掩盖问题"的实质。
  - ✓ **体量大但要说清**：工作副本 ~30GB（`dist/` 16.5GB + `libs/` 13.8GB），但 **git 只跟踪 1522 个文件 / 59.2MB**（捆包 Python 最大单文件 libcrypto DLL 7.61MB），重的是**工作副本与分发产物**而非仓库，且 `dist/` 已在 .gitignore。
  - △ **「文档没看到权限模型」已过时**：那是它 20:20 读的版本，`53c43ab` 之后 README 已有完整权限档位表；缺的只是**沙箱边界 / 合规**这类表述（已补，见下）。
- **本轮落地（按「高价值 + 低风险」取舍）**：
  1. **新增 App 层测试套件** `tests/test_builder_core.py`（标准库 unittest，43 项）+ `tests\run_tests.bat`（自动挑解释器：bundled/venv）。覆盖工具契约（21 工具的数量/名称/schema 形状/required 声明）、权限闸门（5 档 × 新建·覆盖·核心代码·落盘）、批准记忆（文件/目录/全部 + 开关失效）、路径安全（越界/黑名单/白名单/自定义工作区回落）、上下文白名单、manifest 校验（合法 + 8 类问题包）、模块残留清理、磁盘目录浏览、来源链接提取、sidecar 就绪。测试把设置/审批/规则/会话落到 tempdir，**不污染仓库 data/**。
  2. **测试立刻抓出 2 个真实 bug 并修掉**（这就是加测试的价值）：
     - **构建助手读不到自己的主文件**：`CONTEXT_SKIP_DIRS` 没排除 `hf_cache`/`models`/`site-packages` → 成千上万缓存 JSON 挤占 `MAX_CONTEXT_FILES=150` 名额；且单文件上限 48KB < `builder_api.py`（126KB）被静默排除。修：补跳过目录 + 上限提到 160KB。**实测部署版上下文文件从「149 个含大量噪声」变为干净的 46 个**，builder_api.py（126372B / 读取返回 108655 字符）可正常列入与读取。
     - **`_extract_sources` 的正文 URL 兜底依赖 `web_tools`**，该模块不可用时静默返回空 → 模型"凭空引用"。修：新增内置正则 `_urls_in_text()` 兜底（web_tools 优先）。
  3. **README 新增「安全边界与合规」**（回应第 3 条）：权限闸门与工作区限制、HTTP 仅 127.0.0.1 + `SO_EXCLUSIVEADDRUSE` 单实例、密钥/数据仅本机（`ai_providers.json` 不入库）、本机高权限能力（截屏·键鼠·MC）说明、第三方平台（QQ/B 站）自动化涉及 ToS 与风控的提示（**不实现绕过登录/风控**，只用匿名接口）、无担保声明。同时「测试 / 回归」补 App 层说明，目录补 3 个锚点，OVERVIEW 速览补「安全与测试」一条。
  4. **顺手修 UI 瑕疵**：模型下拉在供应商名==模型名时重复显示（`deepseek · deepseek-flash`）→ 只显示一次。
- **有意没做**：259 处静默吞异常**不批量改**（多数是刻意的降级路径，一次改 259 处回归风险不可控）；`libs/` 8.2GB、捆绑 Python 是 standalone 产品的**设计选择**（开箱即用），不动；LICENSE 属法律决定，**交用户定**（已用选择题问）。
- 提交 `ab383da`（6 文件，+545/-8），推送 `79c0958..ab383da`；已同步部署版（`bridge/builder_api.py`、`webui/app.js`、README、OVERVIEW、tests）并重启。

## 用户拍板两项：MIT 许可 + 静默异常「全量分层改造」（提交 98def16）
- 用选择题问了两个决定，用户选：**LICENSE = MIT**；**静默异常 = 全量分层改造（259 处逐个评估，分多轮）**。
- **一、MIT 许可**：`LICENSE`（Copyright (c) 2026 **hth768**，取自 `git config user.name`，如需换真名可改这一行）；README 新增「许可证」节（可自由使用/修改/商用/再分发、唯一要求保留声明、第三方组件另行遵循）+ 目录锚点，并把「安全边界与合规」里「无 LICENSE 声明时默认保留所有权利」改为指向 MIT；OVERVIEW 补一条许可说明。
- **二、静默异常治理基础设施（本轮新建，后续轮次复用）**：
  - `libs/qq_bot_runtime/quiet.py`：`degrade(where, exc, note)` = **刻意降级，默认只计数不打印**（吸取 `cddf082` 日志刷屏教训；`FEIYU_TRACE_DEGRADE=1` 才输出、同站点 5s 合并）；`attention(...)` = 真问题，始终打 `[QUIET][WARN]`（3s 节流）；`snapshot()`/`reset()`/`tracing()`。
  - `tests/audit_silent_except.py`：AST 扫描全仓库静默 handler（有实际处理的不误报）→ 分桶（**A** 探测/回落、**B** 静默失败风险、**C** 待人工）→ 生成 `DEGRADE_AUDIT.md`（统计 + 按文件分布 + **已留痕进度列** + 逐条清单）；`--convert <前缀> --buckets A [--write]` 批量改写。
  - **分桶判定要点**：按 **try 体最外层调用**判断副作用（`open('a','w').write('x')` 的 `write` 才算动作，内层 `open` 不算）——第一版按名字全集判断，把这类误判成"探测"（C），已修。
  - **改写三条硬约束（踩过才写进去的）**：① 必须保留 `except` 行原缩进（丢了会把 `try` 结构写坏 → `SyntaxError`）；② 保留 **CRLF 行尾**与**文件末尾换行**（否则整文件 diff + 行尾混用）；③ 落盘前 `ast.parse` 自检 + B 桶不自动改。
  - 工具自身两次翻车：闭包 `changed` 未 `nonlocal`；`os.path.relpath` **跨盘会抛 ValueError**（测试用 C: 临时文件）→ `_rel()` 兜底。
- **三、第 1 轮改造（App 层 A 桶，43 处）**：`bridge/` 下 app_window 1、appearance_api 3、builder_api 21、memory_api 16、plugins_api 2，每处带 `文件:行 函数` + `降级：<try 首句>`。清单：254 → **213** 剩余（App 层 89→47；A 112→63 / B 71 / C 79）。
- **四、测试**：新增 `tests/test_quiet_and_audit.py`（14 项，覆盖 quiet 默认静默/告警/节流/快照 + 工具的静默判定、分桶、改写正确性含 CRLF/嵌套/缩进/B 桶不动/dry-run）；`test_builder_core.py` 的 `sys.path` 补引擎目录（bridge 现在依赖 `quiet`）。全套 **57 项全过**。
- 部署验证：同步引擎 `quiet.py` + 部署版 `bridge/*`、`tests/*`、`LICENSE`、文档后重启 → `/api/status`、`/api/plugins`、`/api/builder/settings`、`/api/builder/files` 全 200；**启动日志 40 行、异常 0、`[QUIET]` 0 行**（默认不打印，未引入新噪音）。
- 提交 `98def16`（13 文件，+1358/-81），推送 `d7fdf26..98def16`。
- **后续轮次待办**：B 桶 71 处（静默失败风险，需逐个人工判定 degrade / attention / 改抛错）→ C 桶 79 → 引擎层 A 桶。复跑命令：`python tests/audit_silent_except.py --md DEGRADE_AUDIT.md --json data/degrade_inventory.json`。

## 静默异常改造 · 第 2 轮（App 层 B 桶 25 处，提交 8e2b1e4）
- 用户：「继续」→ 按清单做 App 层的 B 桶。从 `data/degrade_inventory.json` 取 25 处（app.py 3 + app_window 1 + appearance 1 + builder_api 9 + loop 2 + pkg_manager 1 + sidecar_runner 3 + server.py 6），逐条打印源码上下文后人工判定。
- **判定原则**（本轮固化，后续轮次沿用）：刻意降级/非关键/客户端断开属常态 → `degrade`；用户或外部能感知的失败（写盘失败、进程终止失败、诊断日志写盘失败）→ `attention`。
- 明细：builder_api 历史/设置/规则/审批/会话的**写盘失败**全部 attention（静默丢落盘用户完全无感），读设置回退默认、删旧备份、流式增量回调为 degrade；server.py 六处写入失败全部 degrade（客户端断开是常态）；sidecar 关日志句柄 degrade、**taskkill 失败 attention**（僵尸进程要能发现）；loop 关循环 degrade；pkg_manager 逐个停止 sidecar degrade；app_window 诊断日志写盘 attention；app.py shutdown 三处 degrade。
- **顺手修的**：`appearance_api._rm_bg` 删除背景文件失败 → attention；`_read` 加注释说明 **bg 以字段为准、不回看磁盘文件**（否则「重置」写 bg="" 后，磁盘残留文件仍会让旧图显示回来）。
- **本轮抓到并修掉自己的一个真 bug**：`app.py` 模块级 `from quiet import degrade` 放在 stdlib 导入之后、bootstrap 之前 —— 而 qq_bot 运行时路径要等 `bootstrap()` 才 `sys.path.insert`，**顶部 import 必然 ModuleNotFoundError，部署实例启动即崩**（同步后冒烟当场抓到：`Traceback ... app.py line 20`）。修：撤掉模块级 import，在 `main()` 里 `bootstrap()` 之后 import（此时路径已就位）。bridge/* 没有这个问题，因为它们本来就是在 bootstrap 之后才被 import 的。**教训：往 app.py 顶部加任何引擎模块的 import 都会踩这个坑。**
- 验证：`py_compile` 全过；57 项测试 OK（含新加的 dry-run 未关闭文件警告修复）；重跑审计 **254 → 186**（App 层 89→21，B 桶 71→46）；部署版同步（11 文件）重启后 `/api/status`、`/api/plugins`、`/api/builder/settings`、`/api/appearance` 全 200，启动日志 40 行 / 异常 0 / `[QUIET]` 0 行。
- 提交 `8e2b1e4`（10 文件，+216/-224），推送 `ff9259a..8e2b1e4`；已同步部署版并重启（PID 15928）。
- **下一轮**：引擎层 B 桶 44 处（`realtime_voice` 9、`plugins_api` 5、`napcat_message/event_api` 6、`mc_bot_brain/mc_event_monitor/mc_agent` 若干、其余引擎模块），再 C 桶 77。
