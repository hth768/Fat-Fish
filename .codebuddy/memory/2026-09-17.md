# 2026-09-17

## AI 供应商 / 模型管理：合并为统一注册表 — 未提交
- 用户需求："将原本的AI供应商栏和新的模型供应商（自定义）合并，保存用户设置的模型，可在配置中切换，也可在构建助手中实时切换"。
- 设计：把原先分散的「槽位式供应商面板（main/vision/role）」与「构建助手硬编码 2 预设下拉」合并为**命名模型注册表**。
- 后端 `bridge/provider_api.py`：
  - 新增 `list_models()`（列出所有命名供应商 + 各能力 active 标记 + 当前路由）、`save_model(payload)`（name/preset/api_style/base_url/api_key/model/capabilities；保存即设为所选能力默认=路由置顶，热重载）、`delete_model(name)`、`set_default(capability, name)`（设某能力默认=置顶路由）。
  - 保留旧 `save_provider(slot)`/`get_provider(slot)`（`provider_api` 历史别名）与 `/api/providers/main|vision|role` 路由（未被新 UI 使用，留作兼容）。
  - 顶部恢复 `from ai_provider import reload_provider_config, load_provider_config`（重写时曾误删，导致 NameError，已修复）。
- 后端 `ai_provider.py`：`chat()` 新增 `provider` 参数——直接指定某命名供应商，跳过全局路由故障转移（用于构建助手「实时切换」，不改全局默认），并校验其 key/能力。
- 后端 `bridge/builder_api.py`：`_llm_json`/`generate_agent`/`generate_plugin`/同步包装 全部透传 `provider`；`server.py` 构建助手路由透传 `provider`。
- 前端 `webui/index.html`：原 `#provPanel`（槽位式）改为统一「AI 供应商（模型管理）」面板——已保存模型卡片列表（含能力标签+默认标记+设为默认/编辑/删除按钮）+ 添加表单（名称/厂商预设/接口类型/BaseURL/Key/模型/自定义/能力多选）。构建助手模型下拉改为由注册表动态填充（含「系统默认」与「自定义模型…」）。
- 前端 `webui/app.js`：新增 `_provCardHTML`/`_provSave`/`_provActivate`/`_provDelete`/`_provEdit` + `#provList` 事件委托；`loadBuilder` 拉取 `/api/providers` 填充下拉；`_bldModelSel` 返回 `{provider, model, think}`，生成时透传 `provider`（自定义模型则置空 provider 用当前默认）。`webui/styles.css` 补充卡片/标签/chip 样式。
- 验证：py_compile OK、node --check OK、read_lints 0 错误；临时脚本确认 save/set_default/list active/chat(provider=)/delete 全链路正确；重启 app（杀掉占用 8900 的真实 PID 38052 后 PowerShell Start-Process 启动）后 `/api/providers` 列表/保存/删除均 200。**尚未提交**。
- 注意坑：浏览器模式 app 重启需先 `netstat -ano | findstr :8900` 找真实监听 PID 再 `taskkill /F /PID`（PowerShell 内联 `$(...)` 会提前插值导致语法错；`start /min` 的 `>` 重定向不捕获子进程日志，应用 PowerShell Start-Process + RedirectStandard*）。

## 删除写死的 DeepSeek 供应商 — 未提交
- 用户需求："将之前写死的deep seek供应商一栏删去"。
- 改动：
  - `settings_store.py`：从配置页「AI 供应商」`CONFIG_SCHEMA` 删除 4 行 DeepSeek 配置（DEEPSEEK_API_KEY/BASE_URL/MODEL/REASONER_MODEL），并从 `PROVIDER_KEYS` 移除 DEEPSEEK_*。
  - `libs/qq_bot_runtime/config.py`：`AI_PROVIDERS`/`AI_CAPABILITY_ROUTING` 默认改为空（不再写死 deepseek），保留顶层 `DEEPSEEK_*` 变量（legacy 客户端/intent_router/reflection 仍引用）。
  - `tests/test_config_center.py`：`test_fallback_no_json` 不再断言默认含 deepseek；`test_overlay_provider_merge_and_env` 改为覆盖层自带完整 deepseek（不再依赖源码默认 base_url）。
- 验证：90 tests passed（含 config center 4 项）；py_compile + read_lints 0 错误。**尚未提交**。

## 修正：删除整个「AI 供应商」配置页栏目（不只是 DeepSeek 子项）
- 用户澄清：上一轮只删了 DeepSeek 子项、留下 GLM/视觉/自动推理，栏目仍在；他要的是**整段「AI 供应商」栏目**消失（供应商改由侧栏注册表管理）。
- 改动 `settings_store.py`：从 `CONFIG_SCHEMA` 删除整个 `{"section": "AI 供应商", ...}` 块（4 子项全删）；`PROVIDER_KEYS` 仅保留 `AI_PROVIDERS`/`AI_CAPABILITY_ROUTING`，移除 GLM/VISION 键。
- 重启 8900 进程（杀旧 PID 26300，PowerShell Start-Process 用捆绑 runtime python 启动 `app.py --browser`，stdout/stderr 必须分两个日志文件，否则报“相同文件”错误）后，`/api/config` 的 sections 仅剩 `人设与语气/记忆系统/知识与联网/聊天行为`，已无 AI 开头的栏目。90 tests 仍全过。
- 坑：PowerShell 内联命令含中文会乱码并破坏字符串（GBK 终端），校验时改用 Python 脚本且避免中文字面量。

## 启动窗口三级降级：APP 独立窗口 > webui > 命令行
- 用户需求：「打开时APP独立窗口优先，webui次之，命令行兜底」。
- 改动：
  - `bridge/app_window.py`：`run_window` 重命名为 `try_app_window`，移除内部浏览器兜底（`_open_default_browser` 删除）；现在只尝试 pywebview → Edge --app，两者都失败返回 `None`。
  - `app.py` 默认分支（无 `--browser`/`--no-window`）：先 `try_app_window`；`None` 则 `webbrowser.open` 降级 webui；再失败则命令行兜底（仅 HTTP 服务，Ctrl+C 退出）。
- 验证：重启监听 8900（PID 32764），日志显示 pywebview 不可导入 → Edge App 窗口已打开（走通独立窗口优先）。py_compile + lint 0 错误。
- 注：`--browser` 仍强制 webui；`--no-window` 仍强制命令行。

## APP 图标：用户提供的蓝发女仆肥鱼娘图设为应用图标
- 改动：
  - 新增 `webui/icon.png`（512）与 `webui/favicon.ico`（16–256 七档多尺寸，RGBA 居中补方）。
  - `webui/index.html` head 注入 `favicon.ico`/`icon.png`/`apple-touch-icon` 三行 link。
  - `bridge/app_window.py`：新增 `ICON_PATH`/`ICON_ICO` 常量；pywebview 分支 `webview.start(icon=...)` best-effort（TypeError 回退无参）。Edge `--app` 模式窗口/任务栏图标自动取页面 favicon，无需额外处理。
- 转换用独立 venv（e:\qq_bot\venv，Pillow 11.1.0；捆绑 runtime 无 PIL）。PowerShell here-string 管道给 python 会带 BOM 报 U+FEFF——须写临时 .py 文件执行后删除。
- 坑：PowerShell `$pid` 是只读自动变量，取端口 PID 要用别的变量名。
- 验证：`/static/favicon.ico` 200 image/x-icon、`/static/icon.png` 200 image/png、首页含 favicon link；app 已重启（Edge App 窗口模式）。

## UI 内「修改图标」功能
- 用户需求：「在UI内增加修改图标的功能」。
- 改动：
  - `bridge/appearance_api.py`：新增 `ICON_PNG/ICON_ICO/ICON_DEFAULT_PNG` 常量；`_to_png()`（PNG 直出；否则用本地 PIL；再否则用独立版 venv `e:\qq_bot\venv\Scripts\python.exe` 的 PIL 子进程兜底，因**捆绑 runtime 无 Pillow**）；`_png_to_ico()`（纯 struct 包裹 PNG-in-ICO，免依赖）；`save_icon(icon_data)`（data URL→PNG 写 webui/icon.png + 生成 webui/favicon.ico）；`reset_icon()`（从 webui/icon_default.png 还原）。
  - `webui/icon_default.png`：由当前 maid 图标 `Copy-Item` 生成的默认备份（供恢复默认）。
  - `server.py`：新增 `POST /api/appearance/icon`（400 返回非法原因）、`POST /api/appearance/icon/reset`。
  - `webui/index.html` 外观页新增「应用图标」panel（预览 img + 文件选择 + 恢复默认）；`styles.css` 加 `.icon-preview`；`app.js` 加 `uploadIcon`/`setFaviconHref` 及上传/重置事件（上传后更新预览与 head favicon 链接带 `?t=` 缓存破，标签页/任务栏即时生效，独立窗口需重启）。
- 验证：捆绑 runtime 跑通 JPG→PNG→ICO 保存+重置（独立 venv PIL 兜底生效）；HTTP POST 保存/重置 200，`/static/icon.png` 重置后恢复 353154 字节默认。py_compile + lint 0 错误。

## 提交（全部改动）
- 用户需求：「提交所有修改，更新readme」。
- 提交 hash `3799098`（本地 main，未推送）。`git add -A` + UTF-8 message 文件（`git commit -F`，规避 PowerShell GBK 乱码；用 Python 生成 message 文件，因 PS 内联中文会乱码）。
- 21 files changed, 917 insertions(+), 177 deletions(-)。含：模型注册表、窗口三级降级、UI 改图标、移除写死 DeepSeek、README 更新、webui/*.ico/*.png 资产、memory 日记。
- 坑：首次提交时 `_commit_msg.txt`/`_commit_msg_gen.py` 被 `git add -A` 误纳入 → `git rm --cached` + `git commit --amend -C HEAD` 移除（amend 前已确认 author 为 hth768，保持一致）。

## 新功能：智能体接入 + 记忆文件导入（未提交）
- 用户需求：「增加智能体接入，记忆文件导入功能」，澄清后两项都要：智能体=本地 agent.json 导入 + 外部 API 接入；记忆=统一 JSON 导入 + 聊天记录解析导入。
- 后端：
  - `bridge/memory_api.py`：新增 `export_memory()`（导出全维度：profiles/notes/persona/reflection/knowledge/history/sessions）、`import_memory(obj, mode)`（mode=replace 覆盖 / merge 合并去重）、`import_chatlog(text, uid, target, mode)`（启发式按句切分写入 knowledge/profile/notes）。
  - `bridge/builder_api.py`：新增 `import_agent_sync`（agent_manager.save_agent(defn)，单参数，id 须内嵌 defn）、`connect_external_agent_sync`（provider_api.save_model 注册 OpenAI 兼容供应商 + agent_manager.save_agent 注册智能体；provider 名 `ext_<slug>`）。
  - `bridge/provider_api.save_model`：放宽——自定义端点(preset 空)允许空 Key（接入免密端点）。
  - `server.py`：新增 `GET /api/memory/export`、`POST /api/memory/import`、`POST /api/memory/import_chatlog`、`POST /api/builder/agent/import`、`POST /api/builder/agent/connect_external`。
  - 注意坑：`agent_manager.save_agent(defn)` 只收 1 个 dict（id 在 defn 内，REQUIRED_FIELDS 含 id）；`provider_api` 须 `from bridge import provider_api`（它在 bridge 包内，不在顶层 path）。
- 前端：`webui/index.html` 记忆页加「记忆导入/导出」面板、构建助手页加「智能体接入」面板（导入 agent.json + 接入外部 API 表单）；`webui/app.js` 绑定事件（导出下载、JSON 导入、聊天记录导入、导入智能体、接入外部）。
- 验证：py_compile + node --check + read_lints 全 0 错误；重启后 5 个端点实测 200（agent 导入/外部接入初测 500，修正签名与导入路径后复测通过）；测试产物已清理（knowledge_base/user_profiles/important_notes 清空、app_settings.json 移除 ext_x）。
- 坑：live 数据文件在 `libs/qq_bot_runtime/`（knowledge_base.json / user_profiles.json / important_notes.json），非 cwd；测试会污染它们，记得清理。尚未提交。
