# 2026-09-16 工作记录

## 修复记忆"写反/串台" + 人格记忆"旧事实优先"
- 用户反馈：写人物档案时用户与AI会写反、记忆串台；写AI人格记忆时应先与现有记忆比较，旧事实确定则否定后面新增的。
- 核查结论："Evan 写死"为假想根因——`Evan` 仅出现在 `config.py:232/393` 注释（主人游戏名 insomnic / B站 UID），属 owner 绑定设计内；chat_service 的命中是 `retrieve_relevant_history` 里 "relev**an**t" 子串误匹配。真正根因是模型在提取记忆时把"用户"与"AI（肥鱼娘）"搞混。
- 改动（两份版本均已同步，均 py_compile 通过）：
  - `chat_service.py` `extract_memory`：提示强化"人物档案只关于用户本人，禁止写 AI 特征"；新增 `user_id` 参数传入提示，让模型明确为谁整理档案。调用点 line ~1578 传入 `user_id=user_id`。
  - `chat_service.py` `extract_persona_from_reflection` 调用改为 `await`（函数已变 async）。
  - `persona_memory.py`：新增 `reconcile_persona_traits(user_id, candidates)`，用 LLM 裁决——旧事实（已确认）优先，与旧事实矛盾的新增特征判定 reject（否定、丢弃），keep 的才 `update_persona`。`extract_persona_from_reflection` 改为 async 并先 reconcile。与 `merge_profile_facts`（用户档案：新覆盖旧）策略相反。
  - `reflection_memory.py` `_REFLECTION_SYSTEM_PROMPT`：开头加主体区分——preference/style/insight/pattern 主体必须是"用户"（人类），correction 主体是"AI"，避免 AI 特征串进用户偏好。
- 涉及两份代码：捆绑版 `e:\feiyu_standalone\libs\qq_bot_runtime` 与 独立版 `e:\qq_bot`。两者同源码，行号略差（extract_memory 捆绑@2009/独立@2000）。
- 已提交并推送：commit `7176037`（amend 自 b323e9a），`origin/main` 7c68046..7176037。仅提交仓库内捆绑版（独立版 `e:\qq_bot` 不属本仓库，用户未要求纳入 git）。
- git 中文坑（已踩并已解决）：PowerShell 5.1 以 GBK 代码页传中文参数给 git，git 默认按 UTF-8 解读 → commit message 乱码存储。正确做法：把 message 写入 UTF-8 文件（用工具，不用命令行），`git commit -F <file>`；amend 修正乱码可用 `git -c i18n.commitEncoding=gbk commit --amend -m "..."`（但 -m 中文含括号等会触发 PowerShell 解析错误，故优先用 -F 文件法）。验证：`chcp 65001` 后 `git log -1 --format=%B` 应正常显示中文。


## 模组世界 FeiyuAPI 模组补充进 release
- 用户要求：模组版 MC 功能需要自建 API 模组，从 `e:\minecraft\PCL 正式版 2.13.1.1\.minecraft\mods\feiyuapi-1.0.0.jar`（NeoForge 1.21.1，已验证可用）补充到 release 并更新 README。
- 该 mod 源码：`libs/qq_bot_runtime/mc_mod/feiyuapi/`（Gradle + net.neoforged.moddev，Minecraft 1.21.1 / NeoForge 21.1.248 / Java 21）。
- release 结构：`dist/` 是发布产物目录；`dist/mc_pack.zip` 是 MC 功能包（内含 `mc_pack/mc_bot/` 纯净版 mineflayer 桥 + `mc_pack/mc_mod/feiyuapi/` 源码）。其他分卷：`feiyu_core.*`、`vl_pack_7z.*`、`voice_pack_7z.*`、`plugins_pack.zip`、`tools_pack.zip`、`napcat_pack.zip`；`dist/SHA256SUMS.txt` 记录各包 sha256。
- 改动：
  1. 复制 PCL 的 jar -> `libs/qq_bot_runtime/mc_mod/feiyuapi/build/libs/feiyuapi-1.0.0.jar`（源码副本保持一致）。
  2. 重建 `dist/mc_pack.zip`：新增 `mc_pack/mods/feiyuapi-1.0.0.jar`，并刷新其中 `mc_pack/mc_bot/README.md`。
  3. 更新 `libs/qq_bot_runtime/mc_bot/README.md`（源码，即 zip 内那份）：新增「## 模组世界（FeiyuAPI 模组）」小节，说明必须插件、随包附带位置、源码与 NeoForge 1.21.1 版本要求。
  4. 刷新 `dist/SHA256SUMS.txt` 中 mc_pack 的 sha256（36d20ab...->c23d0c2...）。
- 用户未要求提交 git（仅"补充到release里并更新readme"），故未 commit；dist/ 在 git 中未跟踪。

## 清理 build/libs 的 feiyuapi jar（只保留 mods/ 分发副本）
- 用户确认：清掉 `build/libs` 那份 jar，只保留 `mc_pack/mods/feiyuapi-1.0.0.jar` 作为分发副本。
- 源码树 `libs/qq_bot_runtime/mc_mod/feiyuapi/build/libs/`：删除 `feiyuapi-1.0.0.jar` 和误提交的垃圾 `untitled-1.ps1`（该 build 目录整体未跟踪，属构建产物）。
- `dist/mc_pack.zip`：用脚本删除条目 `mc_pack/mc_mod/feiyuapi/build/libs/feiyuapi-1.0.0.jar` 与 `.../untitled-1.ps1`，重打包后刷新 `dist/SHA256SUMS.txt`：mc_pack sha256 `c23d0c2...` -> `0e5871abf9ef49c703ba3bac3b7f41ec7a3fe46542db43adf6f33fd4528a2715`。
- 验证：zip 内不再含 build/libs jar，mods jar 仍在。因 dist/ 与 build/ 均未被 git 跟踪，本次为本地发布物调整，无需提交仓库。

## 提交 release 轻量部分入库
- 用户确认范围=仅 release 产物（dist 发布包 + SHA256SUMS.txt + mc_bot/README），并恢复 .gitignore 排除大目录。
- 核查发现 dist/ 无法 git 入库：单文件普遍 ≥100MB（mc_pack.zip 224MB、tools_pack.zip 287MB、分卷各 1.9GB），总量约 16GB，且 GitHub 有 100MB 单文件硬上限 + 体积限制。历史 "release assets published" 实际走 GitHub Releases 附件，原 .gitignore 才忽略 dist/。
- 最终方案（用户确认"仅轻量部分入库"）：`git add -f` 两个文件——`libs/qq_bot_runtime/mc_bot/README.md`（FeiyuAPI 小节）+ `dist/SHA256SUMS.txt`（校验清单，供用户校验 Releases 下载包）；恢复 .gitignore 完全原样（重新忽略 dist/、mc_mod/、mc_bot/、运行时/缓存/安卓等）。
- 提交：commit `aec5790`（chore: 发布 FeiyuAPI 模组说明与校验清单），UTF-8 message 文件 + `git commit -F`，已验证中文无乱码。2 files changed, 98 insertions。
- push 失败：两次均 GitHub 443 连接重置/Could not connect（网络环境问题，非提交问题）。本次提交仅留本地，待网络恢复后再 `git push origin main`。

## 推送成功（SSH 通道，绕过 443 屏蔽）
- 排查：本机无代理，GitHub 443 直连超时（3 次 HTTPS push 全失败）；22(SSH) 端口可达、9418(git) 不通、ping 通。
- 本机原本无 SSH key（`.ssh` 仅 known_hosts）。PowerShell 下 `ssh-keygen -N ""` 报 "Too many arguments"（OpenSSH 对空密码参数解析 bug），最终用 `.bat` 脚本生成成功：`ssh-keygen -t ed25519 -C feiyu_standalone -N "" -f %USERPROFILE%\.ssh\id_ed25519`，公钥 `ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIMMK0+CeSbzZP6s0Ziuh3v74JSkPAJ7JLvXxZv5R4Gkf feiyu_standalone`。
- 用户已把该公钥添加到 GitHub 账号 SSH keys。
- 把 origin 从 HTTPS 改为 SSH：`git remote set-url origin git@github.com:hth768/Fat-Fish.git`（后续推送建议用 SSH，因 443 常态化不可达）。
- 推送：`$env:GIT_SSH_COMMAND='ssh -o StrictHostKeyChecking=accept-new'; git push origin main` 成功，`7176037..aec5790 main -> main`。已 `git log origin/main -1` 验证远端为 aec5790。

## 重建根 README.md 并提交
- 用户要求"根据已上传内容更新 readme"。根 README.md 此前被删（仅 git 索引里仍记为 tracked/已删，工作区无文件）。从 git 读取 `app.py`/`server.py`/`start_app.bat` 确认真实结构后，重建 `e:\feiyu_standalone\README.md`。
- README 内容：肥鱼单机版/肥鱼娘 App 简介、架构图（Python 后端 + pywebview/Edge 窗口 + 原生 webui）、目录结构、启动方式（start_app.bat 便携自举 venv/捆绑 Python；源码用 FEIYU_QQ_BOT）、功能模块、记忆子系统、MC（mc_bot + FeiyuAPI 模组）、发布与安装（dist 走 GitHub Releases、SHA256SUMS 校验）、注意事项。
- 提交 `733d689`（docs: 重建根 README...），UTF-8 message + `git commit -F`，已验证中文无乱码。1 file changed, 83 insertions(+), 117 deletions(-)。
- 推送：`git push origin main` 成功 `aec5790..733d689 main -> main`。

## README 移除独立版内容
- 用户要求"不要把独立版的内容放进 readme"。删除两处独立版引用：①"两份 bot 引擎（捆绑版 vs 独立版 e:\qq_bot）"提示框；②源码运行示例改为本仓库捆绑路径 `libs/qq_bot_runtime`，去掉 `e:\qq_bot`/`f:/qq_bot` 独立版路径。grep 确认无独立版残留。
- 提交 `c1ffaea`（docs: README 移除独立版内容，仅描述捆绑版），推送成功 `733d689..c1ffaea main -> main`。（PowerShell 把 git 的 "To github.com:..." stderr 误判为 RemoteException，实际推送成功。）

## 扩写详尽 README 并提交
- 用户："根据仓库内容和release内容写一份更详细的readme文件"。通读 `libs/qq_bot_runtime/ARCHITECTURE.md`、`mc_bot/README.md`、`plugins/groups.json`、`feiyu-android/README.md`、`dist/SHA256SUMS.txt` 后，对 `README.md` 大幅扩写（227 insertions, 37 deletions）。
- 新增章节：目录、桌面 App 分层 + qq_bot「核心为脑、平台即插件」架构与核心文件职责表、详细仓库结构、配置项表（app_settings/FEIYU_QQ_BOT/AI_PROVIDERS/ENABLE_*）、功能模块展开（聊天大脑/记忆四类子系统/总结/插件分组表/外观）、MC 模组世界(FeiyuAPI)+原版 mineflayer 双身体、安卓版、release 分发包表（含各 sha256）+ 合并分卷与校验安装步骤、FAQ。
- 全程未提及独立版（用户要求"不要把独立版的内容放进readme"）。
- 提交 `7f4cc04`（docs: 扩写详尽 README...），UTF-8 message 无乱码；推送成功 `c1ffaea..7f4cc04 main -> main`。

## 新增插件协议文档 PLUGINS.md
- 用户："如果别人想创建插件，我希望在仓库里给出明确的插件协议，以便插件适配"。
- 新建 `e:\feiyu_standalone\PLUGINS.md`（292 行插入，2 files changed），内容：①两层插件模型（引擎内建 Python 类 / 应用层可插拔包）；②包目录结构；③manifest.json 字段协议（name/title/version/kind/.../sidecar/...）；④五类包契约 platform/feature/brain/sidecar/local 及 create_plugin/create_brain 包装器约定；⑤核心消息/事件契约（InboundMessage/ReplyTarget/MessageSender/AgentEventBus/brain.event）；⑥资源包 NTFS junction 挂载协议；⑦引擎内建插件契约摘要；⑧最小可运行模板（各类型 manifest+plugin.py）。
- README 更新：目录加「插件协议（第三方接入规范）」、插件系统小节加 PLUGINS.md 链接并升级为二级标题章节。
- 提交 `fbac930`（docs: 新增插件协议文档 PLUGINS.md，README 链接），UTF-8 无乱码；推送成功 `7f4cc04..fbac930 main -> main`。

## 多智能体（Multi-Agent）特性（重大改造）
- 用户需求：项目内可同时存在多个智能体，用户自行新建，互不影响，但可在 UI 统一配置。三项决策：
  1. 记忆**完全隔离**（各一套记忆，互不串台）；
  2. **每个智能体独立 AgentCore 实例，惰性启动**（不随程序自启，用户启用/选中/命中绑定才拉起）；
  3. 统一配置面板覆盖：名称/人设/系统提示/模型/启用的 brains 与插件/平台绑定/记忆隔离策略。
- 实现（提交 `7039627`，12 files changed, 300 insertions, 74 deletions；已推送 `79ef201..7039627 main -> main`）：
  - 新增 `agent_manager.py`（阶段1，已先提交 `79ef201`）：智能体注册表，agents/<id>/agent.json，CRUD、match_binding、ensure_default 物化默认 feiyu（肥鱼娘）从 ai_profile 迁移。
  - 新增 `agent_ctx.py`：contextvars 携带当前 agent id；`ns_dir(aid)` 返回 `agents/<id>/memory/`；默认 None→根目录（向后兼容）。
  - 记忆模块（long_term/persona/reflection/important_notes/ai_profile）及 SQLite 后端（fact_store/time_indexed_memory）路径解析按 agent 命名空间；缓存按 agent 分桶（persona/reflection/fact_store/time_indexed）防串台；`_ns_base()` 内 `os.makedirs` 确保目录存在。
  - chat_service / agent_core 持有 agent_id；`handle_message` 包裹 agent 上下文（set/reset token）；`get_chat_service`/`get_core` 按 agent 分桶缓存。
  - 新增 `agent_runtime.py`：多核心管理器 `AgentRuntime`，`match()` 按 bindings 路由、`ensure()` 惰性启动（asyncio 防并发重复）、`submit()` 统一入口设上下文并提交；全局 `get_runtime()`。
  - `.gitignore` 加 `libs/qq_bot_runtime/agents/*/memory/`（运行期隐私数据）。
- 验证：隔离性测试脚本创建 agent_a/agent_b，分别写画像/人格/反思/备忘/AI档案，交叉读取断言互不串台全部 PASSED；各记忆落 `agents/<id>/memory/` 独立目录。
- **注意**：每智能体独立核心目前已在代码层就绪；但 UI 统一配置面板（侧栏"智能体"页：新建/编辑/模型/大脑/插件/绑定/自启）尚未实现，core_bridge 的 submit_chat 也尚未改走 agent_runtime.route（仍默认 feiyu）。这两个是后续集成点。
- **同步提醒**：本次改的是捆绑版 `libs/qq_bot_runtime`（随仓库）。独立版 `e:\qq_bot` 的智能体/记忆/agent_core/chat_service 须在用户确认后同步，否则两份行为不一致。

## 全面代码运行检查（用本地捆绑运行时，无需 PyPI）
- 用户指出：不必从 PyPI 下载依赖，直接用仓库内本地捆绑 Python 即可测试。
- 本地运行时：`e:\feiyu_standalone\libs\qq_bot_runtime\runtime\python\python.exe`（Python 3.13.14）。已装轻量 LLM 依赖（openai 3.14.1 / httpx 0.28.1 / requests / pydantic），**未装** 重依赖（numpy/cv2/mss/PIL/websockets/torch/sounddevice 等）。
- 验证结果：①测试套件 `tests/run.py` 本地运行时跑通，**90 测试全 OK**；②`python -m compileall` 整个引擎目录零语法错误；③核心模块（config/记忆/agent/插件API/遥测/chat_service）均可导入。
- 修复：`chat_service.py` 原本在顶层 `from vision_capture import vision_capture` 导致 `import cv2` 硬阻断核心导入。改为与既有的 `mc_watcher` 一致惰性降级（try/except 置 None + 视觉指令统一守卫 + 实时视觉注入加 None 守卫）。视觉功能(c2/mss)仅在真正使用时才需这些依赖，核心对话不再被卡。
- 结论：核心引擎/记忆/多智能体/插件/遥测/对话流水线 均可在本地无网络环境测试；仅 实时视觉、QQ机器人(websockets)、语音(numpy)、本地视频理解(torch) 这几类重依赖功能需另行装包才能端到端跑（且均已惰性导入，不阻断核心）。audit 修复（web_plugin/memory/config/tests/settings_store 还原）仍处未提交本地改动。

## 新增「扩展设置」：每个插件可在 UI 配置参数（用户需求）
- 路径：恢复 server.py/app.py（之前被删），在 bridge + 根 webui 实现（非 web_plugin.py 那条）。已确认 bridge 才是桌面App后端并服务根 webui。
- 机制：插件在 manifest.json 声明 `config_schema`（字段 key/label/type/desc/default/options）；支持 str/text/int/float/bool/choice/secret/json。UI 在插件页按 schema 动态渲染表单。
- 后端：GET/POST `/api/plugins/<name>/config` → bridge/plugins_api.plugin_config_view/save。保存落到 settings_store 的覆盖层 `plugin_config` 子键（data/app_settings.json），并热推插件 wrapper 的 `on_config(cfg)`（pkg_manager 记录已加载模块 + get_config/set_config）。secret 类型掩码显示、留掩码则保留原值。
- 前端：webui 插件表每行加「设置」按钮（has_config 才显示），弹窗动态表单；bindPluginEvents 绑定 + DOMContentLoaded 绑弹窗按钮。styles.css 加 .modal 样式。
- 文档：PLUGINS.md 新增第 9 节 config_schema 协议（含 on_config 钩子、后端约定）。
- 验证：用捆绑运行时跑 _cfg_test.py（临时 SETTINGS_FILE 隔离，未污染仓库），settings_store 往返 + view默认值 + 类型转换 + secret掩码 + 热生效 + 保留secret 全 OK；app.js node --check 通过；已清理测试期误写入 data/app_settings.json 的 demo/store_demo 污染。
- 重要坑：settings_store 硬编码 DATA_DIR=APP_DIR/data（无 APP_DATA_DIR 环境变量），测试须直接覆盖 settings_store.SETTINGS_FILE 才能隔离。

## 用独立版 venv 做全量端到端验证（用户建议：用独立版里的依赖）
- 独立版环境：`e:\qq_bot\venv\Scripts\python.exe`（Py 3.11.x），按 `e:\qq_bot\requirements.txt` 已装 cv2(opencv-python)/mss/numpy/Pillow/websockets/torch/torchvision/sounddevice/httpx/requests。**缺 openai**，但捆绑仓库引擎模块不直接 `import openai`（走 ai_provider），故不影响导入/测试。
- 用它作为解释器跑**捆绑仓库** `libs/qq_bot_runtime`：
  - 测试套件 `tests/run.py`：**90 测试全 OK**。
  - 全量导入：87 个引擎模块 **84 个成功**，仅 3 个失败且均为调试脚本（`_dbg_mem_tmp`/`_dbg_mem2_tmp` 硬编码 `f:\qq_bot\_dbg_mem*.log` 路径；`_run_monitored` 依赖可选 `mc_agent`）。所有真正引擎模块（chat_service/vision_capture/local_video_understand/gpu_pipeline/bili_streamer/qq_plugin/realtime_voice/tts_vox/web_plugin/整套 agent_*）均可导入运行。
- 结论：无需 PyPI，仅靠独立版 venv 的现成依赖即可完成仓库全部代码运行检查。视觉/QQ/语音/视频理解等重依赖功能在独立版 venv 中全部可用。
- 提醒：独立版 `e:\qq_bot` 是较旧副本，仅借用其依赖；审计对象仍是捆绑仓库 `libs/qq_bot_runtime`，勿把独立版代码当最新。

## 提交（f42db2d，已 push 到 origin/main）
- 用户说"提交"。按仓库中文规范：写 UTF-8 信息文件 `_commit_msg.txt` + `git commit -F`，已删除临时文件。远程 SSH `git@github.com:hth768/Fat-Fish.git`，推送成功 `7039627..f42db2d`。
- 精实名 add 11 个文件（8 个审计代码 + 3 个 .codebuddy/memory）：chat_service/config/persona_memory/reflection_memory/tests(3)/web_plugin + 记忆文件。
- **未提交**（需用户另行决定）：① 根目录旧文件删除 FeiyuApp.exe/_launcher.py/app.py/server.py/start_app.bat/_*.py/*.bat（原本已入库，提交即结构性移除）；② 未跟踪巨量项 libs/qq_bot_runtime/runtime/python/Lib/site-packages/、Scripts/*.exe、feiyu-android/、垃圾脚本(_deps/_smoke/_pip*.err 等)、memory_session_*.json 测试产物。**未用 git add -A**，避免 site-packages/feiyu-android 撑爆仓库。

## 提交「扩展设置」功能（f9e5174，已 push 到 origin/main）
- 用户说"这次扩展设置相关改动一起提交"。仅精实名 add 8 个功能文件，未纳入无关的旧文件删除与记忆文件。
- 提交 `f9e5174`：PLUGINS.md / bridge/pkg_manager.py / bridge/plugins_api.py / server.py / settings_store.py / webui/app.js / webui/index.html / webui/styles.css（359 insertions, 1 deletion）。UTF-8 message 文件 + `git commit -F`，已验证中文无乱码。推送成功 `f42db2d..f9e5174 main -> main`。
- 注：app.py 已跟踪未改动（无需恢复/提交）；server.py 改动即扩展设置 GET/POST `/api/plugins/<name>/config` 路由。工作区仍残留未跟踪：FeiyuApp.exe/_*.py/*.bat 删除、site-packages/feiyu-android/垃圾脚本等，保留待用户决定。

## 清理仓库（4f053e4，已 push 到 origin/main）
- 用户说"把不用的文件清理出仓库"。
- 提交 `4f053e4`：12 files changed, 19 insertions(+), 1107 deletions(-)。UTF-8 message + `git commit -F`，中文无乱码；推送成功 `f9e5174..4f053e4 main -> main`。
- 两类清理：① 暂存并提交 11 个已从磁盘删除的旧文件（git 记录里移除）：FeiyuApp.exe、_bridge_unpack/_launcher/_make_packs/_progress_gui/_split_release/_vox_setup/_zip_worker.py、setup_vox.bat、start_app.bat、nul; pyc。② 物理删除根目录临时脚本/测试产物：_deps.py/_pip*.err/_settings_store_recovered.py/_smoke*.py/_syntax_check.py/_test_out.txt、libs/qq_bot_runtime/memory_session_*.json、telemetry_server_test.json。
- `.gitignore` 补充：libs/qq_bot_runtime/runtime/python/Lib/site-packages/、Scripts/、feiyu-android/、临时脚本/测试产物。确保后续不误提交。
- **未纳入**：.codebuddy/memory/*（按惯例保留工作区未提交）。site-packages/feiyu-android 仅 gitignore 保留本地，不入库（体积巨大/构建产物）。

## 新增「构建助手」功能（内置 Agent：生成智能体/插件，未提交）
- 用户需求：软件内置一个辅助用户构建智能体或写插件的 Agent，产物要能适配系统。确认：两套产物都做、产物要真实产出代码、严格校验后落盘。
- 架构落地：
  - `bridge/builder_api.py`（新增）：`_llm_json` 调 `ai_provider.get_llm().chat(capability="chat")`，提示词内嵌 agent.json schema 与 PLUGINS.md 契约，保证"适配"。校验：`_normalize_agent`（→agent_manager.save_agent 必填/默认值）、`_normalize_plugin`（name 蛇形/VALYDS_KINDS、manifest 字段、config_schema 结构）、`_check_plugin_code`（tempfile + py_compile + AST 检查 create_plugin/create_brain 入口）。落盘：`save_agent`（生成 slug+时间戳 id，写 libs/qq_bot_runtime/agents/<id>/agent.json）、`save_plugin`（写 plugins/<name>/{manifest.json,plugin.py} 后调 plugins_api.rescan）。同步包装 `bridge.lt.run_coro(...)`（沿用 summary_api 模式）。
  - `server.py`：导入 builder_api，do_POST 新增 `/api/builder/agent|plugin/generate` 与 `/api/builder/agent|plugin/save`。
  - `webui/index.html|app.js|styles.css`：侧栏「构建助手」页；模式切换(seg)、需求输入、生成、可编辑预览(agent.json 单框 / manifest+plugin.py 双框)、保存/复制。bindings 在 DOMContentLoaded；loaders 映射加 builder。
- 验证：bundled python `py_compile` server.py+builder_api OK；桥接 namespace 包导入 OK；单元测 _normalize/_check_plugin_code/JSON 提取全部通过（good→None，bad name/kind/缺入口→明确报错）；`node --check app.js` OK；read_lints 0 错误。
- 注意：LLM 调用依赖已配置 AI 供应商(api_key)，未配置时 generate 返回明确错误。保存智能体需 import agent_manager（运行时 libs/qq_bot_runtime，App 进程 sys.path 已含）。**尚未提交**，待用户决定。

## 构建助手补全「自愈式校验闭环」+「干加载校验」+ few-shot（未提交）
- 用户问"内置 agent 能否按用户要求生成完整插件代码"→ 诚实答：之前只到 py_compile+AST 入口检查，不真导入/不实例化，且提示词把入口写成 `create_plugin(dir)` 是错的（实际 pkg_manager 调 `create_plugin(core)`/`create_brain(core)`）。用户说"帮我补全"。
- 已补全 `bridge/builder_api.py`：
  - **自愈闭环**：`generate_plugin`/`generate_agent` 改为循环（插件最多 3 轮、智能体 2 轮）——生成→`_normalize_plugin`→`_dry_load_plugin`→若失败把错误回喂 LLM 重生成，直到通过才返回 `{"ok":True,"rounds":n}`。
  - **干加载校验 `_dry_load_plugin`**：`tempfile` 落 `plugin.py` → `importlib.spec_from_file_location` 真导入（含依赖检查）→ 调 `create_plugin/create_brain(_FakeCore())` 实例化 → `isinstance` 校验返回类型（feature/local/platform→Plugin；brain→AgentBrain）→ platform 还检查类属性 `platform` 不能是基类默认 `"base"`。等价于 pkg_manager 装载最关键环节，保证落盘即可适配。失败信息回喂 LLM。
  - **few-shot**：`PLUGIN_SYSTEM` 重写，修正入口签名为 `create_plugin(core)`，补最小可用 feature/brain 插件示例 + platform 要点。
  - `save_plugin` 落盘前也加 `_dry_load_plugin` 兜底；`generate_*` 同步超时放宽到 300/120s。
  - 关键：_FakeCore 用 __getattr__ 占位，覆盖插件构造期碰到的 core.xxx，避免误判。
- 验证：bundled python `py_compile` server.py+builder_api OK；`node --check app.js` OK；read_lints 0 错误；单元测 _dry_load_plugin 全过（good feature/brain→None；bad 返回类型/坏 import/缺入口/platform 未覆盖 均明确报错）；前端 _bldGenerate 显示"✓ 已通过系统干加载校验（编译→导入→实例化→契约检查）"及自校正轮次。**尚未提交**，待用户决定。
- 边界：干加载不调用 start()（仅实例化），故 async 启动逻辑未跑；仍需用户实际启用验证运行时行为。plugin_base.PlatformPlugin 自带 `platform="base"` 默认，故检查针对"仍为 base"报错。

## 构建助手增加「模型/思考强度」选择（未提交）
- 用户要求："增加模型思考强度选择的功能，模型种类预设 deepseek-flash 和 deepseek-v4-pro"。
- 实现：构建助手 UI 增加下拉 `#bldModel`，两个预设 `deepseek-flash`（快速·轻思考，think=False）/ `deepseek-v4-pro`（深度·强思考，think=True）。生成请求带 `model`+`think` 透传到 `chat()`；v4-pro 自动开 `think`。
  - `bridge/builder_api.py`：`_llm_json(system,user,model=None,think=False)` → `get_llm().chat(..., model=model or None, think=bool(think))`；`generate_agent/generate_plugin` 与 `*_sync` 透传 model/think。
  - `server.py`：两生成路由读 `body.get("model")`/`bool(body.get("think"))` 透传。
  - `webui/index.html`：`#bldModel` 下拉（放进需求输入下方 panel）。
  - `webui/app.js`：`BLD_MODELS` 映射 think；`_bldModelSel()` 取 model+think；`_bldGenerate` 请求带 model/think；`loadBuilder` 用 localStorage 恢复上次选择（`bld_model`）。
- 验证：py_compile server.py+builder_api OK；`node --check app.js` OK；read_lints 0 错误；单测确认 model/think 正确透传到 chat（plugin: deepseek-v4-pro/True 通过；agent 调用同样透传 deepseek-flash/False，仅测试桩 JSON 形状不符 agent schema 故 ok=False，非 bug）。**尚未提交**。
- 注意：`think` 是布尔开关（ai_provider.chat 仅支持 bool），故"思考强度"用模型预设区分；若需更细强度需后端扩展。模型名作为显式 `model` 最高优先级传给 provider，假设已配 deepseek。

## 主模型供应商配置（多厂商/预设/自定义/OpenAI+Anthropic 兼容）— 未提交
- 用户需求："主模型可以使用不同厂家的，提供集中常用模型的API预设，APIkey由用户提供，可自定义模型，支持openai和Anthropic兼容"。
- 实现：
  - `libs/qq_bot_runtime/ai_provider.py`：`_call_once` 按 `prov.get("api_style")` 分流（"openai" 默认 / "anthropic"）。新增 `_call_anthropic`+`_build_anthropic_payload`+`_inject_images_anthropic`：Anthropic Messages API（`{base_url}/v1/messages`，header `x-api-key`+`anthropic-version:2023-06-01`，body `model/max_tokens/messages/system/thinking/tools`，解析 `content[].text` 与 `tool_use`→OpenAI 风格 tool_calls）；think 经 `thinking:{type:enabled,budget_tokens}` 且调高 max_tokens；tools 做 OpenAI↔Anthropic 双向转换；图片走 image 块。原来只有 OpenAI 兼容。
  - `bridge/provider_api.py`（新增）：`PROVIDER_PRESETS`（openai/anthropic/deepseek/moonshot/qwen/glm/custom，各含 api_style+base_url+常用 models）；`list_presets`/`get_main`/`save_main`。`save_main` 写 `config.AI_PROVIDERS["main"]`（api_key/base_url/default_model/api_style/models.capabilities）+ 重建 `AI_CAPABILITY_ROUTING` chat/reasoning=["main", 其余带 key 供应商作故障转移]，并 `set_values` 写覆盖层 + `apply_value` 热生效 + `ai_provider.reload_provider_config()`。密钥留掩码（"****"）则不改动（is_masked 判断）。
  - `server.py`：导入 provider_api；`do_GET` 加 `/api/providers/presets`、`/api/providers/main`；`do_POST` 加 `/api/providers/main`（save_main）。
  - `webui/index.html`：设置页 `#provPanel`（厂商预设/接口类型/BaseURL/APIKey/模型/自定义模型名 + 保存）。
  - `webui/app.js`：`loadProviders()`（get_main 填充预设+当前配置，不回显 key）、`_provPresetChange`(选预设自动填 base_url/style/models)、`saveProvider`（POST，含掩码判断）、DOMContentLoaded 绑定；`loadConfig` 内调用 loadProviders。
- 验证：py_compile ai_provider/server.py/provider_api OK；node --check app.js OK；read_lints 0 错误；单测：anthropic payload 构造（model/system/messages/think/tools 转换）、presets 7 项、save_main 路由 main 优先+deepseek 后备、掩码 key 保留、缺 key 报错 全部通过。**尚未提交**。
- 注意：旧 flat schema 的 `DEEPSEEK_*` 仍可用；main 供应商入路由后成为主模型（chat/reasoning）。vision/role 路由不覆盖。anthropic 的 max_tokens 必填已处理。

## 自定义供应商扩展到 视觉/角色 模型 — 未提交
- 用户需求："将自定义模型供应商功能都加进视觉/角色模型"。即把上一节仅覆盖主模型(main)的预设/Key/自定义模型/OpenAI+Anthropic 能力，扩展到「视觉模型」与「角色模型」两个槽位。
- 设计：在 `bridge/provider_api.py` 引入 `SLOT_DEFS`（main/vision/role），每个槽位定义 `prov_name`/`caps`/`routing_caps`/`cap_models`/`role_routing`。`save_provider(slot,payload)` 通用化：写对应 `config.AI_PROVIDERS[prov_name]`（capabilities 按槽位，如 vision→["vision"]、role→["role"]、main→["chat","reasoning"]）+ 仅重建该槽位相关能力的路由（本供应商优先，其余带 key 供应商兜底，其它能力路由原样保留）+ `role_routing["*"]`（角色通配，仅 role 槽位）+ `set_values`/`apply_value`/`reload_provider_config`。`get_provider(slot)` 返回该槽位当前配置（不回显明文 key）。保留 `get_main/save_main` 作为 main 别名。
- 后端配套：
  - `ai_provider.py`：`chat()` 角色路由增加 `"*"` 通配回退（`role_routing.get(role) or role_routing.get("*")`），使未显式配置的角色（judge/memory_extract/summary…）统一走 role 槽位模型。`UnifiedVision` 新增 `_use_llm_vision`：仅当用户通过「视觉模型」槽位配置了名为 `vision` 且含 key 的供应商（即 `capability_routing["vision"]` 含 "vision"）时，单图 `describe_image` 才优先走 `get_llm().describe_image(capability="vision")`（OpenAI/Anthropic 图片理解），失败回退 GLM/Gemini 专用驱动。**默认包不触发**（默认 vision 路由为 ["deepseek","gemini"]，无 "vision" 供应商），故向后兼容、不回归既有视觉行为。gif/emoji/帧等专用驱动路径不受影响（仍走 GLM/Gemini）。
  - `server.py`：`do_GET`/`do_POST` 的 `/api/providers/main` 升级为 `("/api/providers/main","/api/providers/vision","/api/providers/role")` 三元分发，按 path 末段取 slot。
- 前端：`webui/index.html` 供应商面板加「模型槽位」下拉（主/视觉/角色）；`webui/app.js` `loadProviders(slot)`/`saveProvider` 改为按 `currentProvSlot` 加载与 POST 到对应端点，槽位切换重加载该槽位配置。
- 验证：py_compile ai_provider/server.py/provider_api OK；node --check app.js OK；read_lints 0 错误；临时测试脚本确认三槽位路由正确（vision→cap[vision]=["vision"]、role→cap[role]=["role"]+role_routing["*"]、main→cap[chat/reasoning]=["main"]），chat(role=judge) 经 "*" 命中 role 模型、chat() 无 role 走 main。**尚未提交**。
- 边界：视觉槽位只接管单图 describe_image（经 ai_provider 的 vision 能力，支持多模态模型如 gpt-4o/claude-3.5/qwen2.5-vl）；专用 gif/emoji/视频帧理解仍由 GLM/Gemini 驱动，若要统一需另改视觉专用驱动，本次未做。
